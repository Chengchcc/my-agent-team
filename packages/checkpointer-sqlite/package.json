{
  "name": "@my-agent-team/checkpointer-sqlite",
  "version": "0.1.0",
  "description": "SQLite-backed Checkpointer implementation (bun:sqlite)",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "biome check . && eslint .",
    "test": "bun test --pass-with-no-tests",
    "typecheck": "tsc -p tsconfig.test.json --noEmit"
  },
  "dependencies": {
    "@my-agent-team/framework": "workspace:*"
  },
  "devDependencies": {}
}
