{
  "name": "@my-agent-team/plugin-conversation-context",
  "version": "0.1.0",
  "description": "Conversation context plugin — injects conversation info and tools into agent runs",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "biome check . && eslint .",
    "test": "bun test --pass-with-no-tests",
    "typecheck": "tsc -p tsconfig.test.json --noEmit"
  },
  "dependencies": {
    "@my-agent-team/core": "workspace:*",
    "@my-agent-team/framework": "workspace:*",
    "@my-agent-team/message": "workspace:*"
  }
}
