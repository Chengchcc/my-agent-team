{
  "name": "@my-agent-team/api-contract",
  "version": "0.1.0",
  "description": "Shared API contract types (HTTP App, SSE event maps) — zero runtime deps, type-only",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "biome check . && eslint .",
    "test": "bun test",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "elysia": "^1.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {}
}
