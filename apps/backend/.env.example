# Backend — required (one of ANTHROPIC_API_KEY or ANTHROPIC_AUTH_TOKEN)
ANTHROPIC_AUTH_TOKEN=            # <-- YOUR API KEY
# ANTHROPIC_API_KEY=sk-ant-...   # alternative: standard Anthropic key
BACKEND_AUTH_TOKEN=dev-token     # <-- pick a random string, share with apps/web

# Backend — optional (defaults shown)
# ANTHROPIC_BASE_URL=https://api.anthropic.com   # API proxy / alternative endpoint
# BACKEND_PORT=3000
# BACKEND_HOST=127.0.0.1
# BACKEND_DATA_DIR=./.backend-data
# BACKEND_MAX_CONCURRENT=8

# Production override: absolute path to the built oma CLI (or a deployment
# wrapper). Leave empty in monorepo development: the Backend runs the source
# entry (apps/oh-my-agent/src/cli.ts) with its own Bun, no global install.
# OMA_BIN=

# Debug: emit the end-to-end run lifecycle chain to stderr (stage names,
# ids, counts only - no message/tool/prompt content, secrets redacted).
# OMA_DEBUG=1

# Workflow script nodes execute unattended code — opt-in (H2).
# When enabled, each script also runs under bwrap/sandbox-exec when
# available: no network, deny-read over BACKEND_DATA_DIR and .env.
# WORKFLOW_SCRIPTS_ENABLED=1

# Extra paths the workflow script sandbox must not read (comma-separated,
# H2). dataDir and the deployment .env are always denied. The sandbox also
# hides /root and /home and cuts network + host process visibility.
# WORKFLOW_SANDBOX_DENY_READ=/srv/secrets,/home/me/private
