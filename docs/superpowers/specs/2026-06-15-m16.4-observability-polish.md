# M16.4 — Observability Console UI/UX 收口

> **Status:** Spec → Implementation in progress.
> **Baseline:** `2c937be` (M16.3 review fixes applied: token normalization, chart sizing, waterfall bars).
> **关联:** [M16.3 Run Insights](./2026-06-15-m16.3-run-insights.md)

## 一句话定位

Parts A/B/F 已在 M16.3 review 阶段完成。本期专注于 Part C（反馈闭环）+ Part D（排查动线）+ Part E（流程细节）——全前端，零后端改动。

---

## 已完成（M16.3 review 阶段，commits f24140e..2c937be）

| Part | 内容 | 状态 |
|------|------|------|
| A | 全站裸色→token（8 文件）、AgentForm 浅色徽章 | ✅ `978510c` |
| A | ToolResultCard 错误态红色 | ✅ `978510c` |
| B | 图表 aspect-video→固定/动态高度 | ✅ `f24140e` |
| B | TraceWaterfall 条宽反映 duration | ✅ `cd16e0f` |
| B | Diagnosis 列按 kind 着色 + agentName truncate | ✅ `978510c` |
| B | Timeline 节点圆点、responsive grids | ✅ `66ec32e` |
| B | shadow→border、Markdown 表格边框、滚动按钮 | ✅ `2c937be` |
| F | unknownCostCalls 前端展示 | ✅ `49405bb` |

---

## 待实现

### Part C — 反馈闭环（最高杠杆）

1. **挂载 Toaster** — `AppShell.tsx` 或 root layout 挂 `<Toaster/>`
2. **全站 mutation 接 toast** — RunControlStrip (cancel/recover)、NeedsAttentionList (recover)、AgentForm (create/edit)、AgentList (archive)、ConversationCanvas (remove member)、ConversationList (delete)、useConversation (send fail)
3. **危险操作二次确认** — cancel run、remove member、archive agent、delete conversation、sign out → Dialog

### Part D — 排查动线

1. 概览计数卡可下钻（HealthSummary 数字→筛选后列表）
2. trace↔run 互跳链接
3. 长 ID 一键复制
4. 空态带 CTA

### Part E — 流程细节

1. 流式可中断中间态修复
2. 筛选状态入 URL search params
3. 登录页受控提交 + 失败反馈
4. a11y（aria-pressed、aria-label）
