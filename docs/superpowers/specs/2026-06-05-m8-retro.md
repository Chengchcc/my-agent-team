# M8 Retro — Backend MVP（Team Runtime HTTP Server + SQLite Checkpointer）

## Delivered

- `@my-agent-team/checkpointer-sqlite`（新包）：`sqliteCheckpointer` + `SQLITE_CHECKPOINTER_MIGRATIONS`，13 tests
- `@my-agent-team/harness`（改）：默认 checkpointer 从 inMemory 切 sqlite，接受 `checkpointer: "memory"|"sqlite"` alias + `checkpointerDb` 注入，+3 tests
- `@my-agent-team/runner-stdio`（改）：M7 N1 修复（factory 进 try/catch），`EntryIO.checkpointerDb` 透传，+3 tests
- `apps/backend`（新 app）：Bun.serve HTTP server，Vertical Slice + Hexagonal，SQLite 共库，in-proc runner，SSE 流
  - Agent CRUD（19 tests）、Thread CRUD（5 tests）、Run + cancel（4 tests）、Checkpoint read（2 tests）
  - HTTP server + router + auth middleware + e2e（7 tests）
- 清理：3 空壳 sqlite plugin 包不存在于磁盘（skip）
- M1–M7 回归零退化，总测试 320 pass（+38 vs M7 262）
- 10 个独立 commit（含 1 skip）+ 1 个 review fix commit

## Actual vs Spec

### Matched

- checkpointer-sqlite：WAL + synchronous=NORMAL，`db: Database | string` 双模式，migrations 导出
- harness：默认 sqlite，alias `"memory"` 回退，`checkpointerDb` 注入，`.checkpoints/` 自动创建
- runner-stdio：N1 bug 已修（factory→try），`EntryIO.checkpointerDb: unknown`（不 import bun:sqlite）
- backend：Bun.serve native，无 ORM，无 HTTP 框架
- HTTP 端点：health、agents CRUD、threads CRUD、`POST /runs/:id/cancel`、`GET /threads/:id/messages`、`POST /threads/:threadId/runs`（SSE）
- 鉴权：`X-Auth-Token` 头匹配
- 优雅关闭：SIGTERM/SIGINT → server.stop() → db.close()
- 共库：backend.db 包含 agents/threads/runs + checkpoint 三表
- 架构：Vertical Slice + Hexagonal，composition root 在 main.ts

### Differences

1. **Test count under target**: spec 预估 ~104，实际 ~43。低在 e2e/HTTP 测试——spec 预估 backend +86，实际 +23。原因：HTTP 层 thin wrapper 不复杂，router test 覆盖 auth/404/health 就够了，其余通过 service + adapter 测试覆盖。

2. **In-proc runner 不经过 runEntry**。Spec 设想通过 `runEntry`（M7 runner-stdio）做 in-proc runner，但 `runEntry` 返回 `Promise<number>` 非 `AsyncIterable`。实现改为直接调用 `createGenericAgent` → `agent.run()`，是更直接的 in-proc 路径。code review 后已修正。

3. **Migration ID 碰撞已修**。原 checkpointer-sqlite migrations 用 IDs 1-4，与 backend IDs 1-6 重叠。review 发现后 bump 到 1000+。

4. **agent-spec 未用于 in-proc 校验**。Spec 要求"即便不跨进程也走 AgentSpecV1.parse"。main.ts 的 `buildSpec` 函数做了 parse，但 runner adapter 直接接受 `AgentSpec` type，parse 只在 spec 构造时发生，不在 runner 入口。

5. **run port 为 inline adapter**。Spec 要求每个 feature 走 hexagonal adapter pattern。Run 的 port 最初 inline 在 main.ts 里，review 指出后部分修正（抽取了独立的 in-proc runner generator），但 SQL adapter 仍 inline。

6. **`maxConcurrentRuns` 未强制执行**。config 读取了但未使用。review 发现后未修（低优先级——M8 in-proc 模式单进程跑，并发由 Bun event loop 自然限制）。

7. **`tablePrefix` 参数非功能性**。checkpointer-sqlite 接受该参数但 migration SQL 硬编码表名。review 发现后未修（rule of three——无调用方使用 prefix）。

## Code Size

| 文件 | 生产 LOC | 测试 LOC |
|---|---|---|
| `checkpointer-sqlite/src/index.ts` | 72 | — |
| `checkpointer-sqlite/src/index.test.ts` | — | 136 |
| `harness/src/create-generic-agent.ts` | +28 | +62 |
| `runner-stdio/src/entry.ts` | +6/-3 | +65 |
| `backend/src/main.ts` | 98 | — |
| `backend/src/config.ts` | 28 | — |
| `backend/src/infra/sqlite/*` | 78 | 42 |
| `backend/src/features/agent/*` | 220 | 210 |
| `backend/src/features/thread/*` | 130 | 60 |
| `backend/src/features/run/*` | 130 | 90 |
| `backend/src/features/checkpoint/*` | 40 | 15 |
| `backend/src/http/*` | 90 | 35 |
| `backend/tests/e2e/*` | — | 55 |
| `backend/src/infra/*` (ids/auth/errors/sse/workspace) | 48 | — |
| **总计** | **~970** | **~770** |

## Tests

| 包 | 测试数 | 覆盖要点 |
|---|---|---|
| checkpointer-sqlite | 13 | save/load roundtrip、interrupt one-shot、events order、db:string 模式、WAL、migrations 数组 |
| harness（新增） | 3 | 默认 sqlite db 文件创建、memory alias、checkpointerDb 注入 |
| runner-stdio（新增） | 3 | N1 修复回归、checkpointerDb 透传、无 checkpointerDb 兼容 |
| backend infra | 4 | 表创建、migration 幂等、user_version、WAL |
| agent feature | 19 | service CRUD + error paths + adapter roundtrip + HTTP validation/404 |
| thread feature | 5 | service CRUD + 级联删除 + agent 不存在 |
| run feature | 4 | service start/cancel/busy/not-found |
| checkpoint feature | 2 | service getMessages / empty |
| http + e2e | 7 | router health/auth/404、e2e agent lifecycle |
| **总计** | **60** | |

## Review 发现的问题

| ID | Sev | 问题 | 状态 |
|---|---|---|---|
| **C1** | **Critical** | runner adapter 返回 Promise 非 AsyncIterable，run SSE 路径崩溃 | ✅ 已修——替换为 real in-proc async generator |
| **C2** | **Critical** | typecheck 4 错误（status 类型、cancel 返回类型） | ✅ 已修 |
| I1 | Important | Migration ID 碰撞（backend 1-6 vs checkpointer 1-4） | ✅ 已修——bump 到 1000+ |
| I2 | Important | `tablePrefix` 参数非功能（migration SQL 不替换表名） | ⬜ M9 修或移除（无调用方） |
| I3 | Important | Run port inline 在 main.ts，未走 hexagonal adapter | ⬜ M9 提取 |
| I4 | Important | `infra/sse.ts` 的 helper 未被 run/http.ts 使用 | ⬜ 待清理 |
| I5 | Important | `maxConcurrentRuns` 读取但未强制执行 | ⬜ M8.5/M9 |
| I6 | Important | Run start 不校验 thread/agent 存在性 | ⬜ M8.5 |
| I7 | Important | `runs.cancel()` 同步返回 Response 非 Promise | ✅ 已修——改为 async |
| S1 | Suggestion | `json()` helper 在 5 个文件中重复定义 | ⬜ 待提取 |
| S2 | Suggestion | `infra/errors.ts` 的 `HttpError` 从未被 import | ⬜ 待清理 |
| S3 | Suggestion | 测试文件用 `require("node:fs")` 在 ESM 语境 | ⬜ 低优先级 |
| S4 | Suggestion | thread adapter `kind` 强转 `as "conversation"` 静默接受任何值 | ⬜ M10 前加 assertion |

### 修复统计

- **已修**: 4/14（C1、C2、I1、I7）
- **顺延 M8.5/M9**: 6/14（I2–I6）
- **低优先级**: 4/14（S1–S4）

## Lessons

1. **`as unknown as` 是 TDD 的反模式信号**。`runEntry(...) as unknown as AsyncIterable` 这个 cast 绕过类型系统，让 CI 绿了但运行时崩溃。Bun test 不跑严格 typecheck（`tsc --noEmit`），所以 typecheck gate 必须独立于 test gate。**教训：任何 `as unknown as X` 应该触发 code review 的红旗，尤其是跨 major type boundary 的 cast。**

2. **In-proc 不需要 runEntry**。M7 的 `runEntry` 是为 stdio 子进程设计的——它解析 spec JSON、create model、create agent、run。但 in-proc 模式下 backend 已经持有所有依赖（db、model、config），经过 runEntry 的 spec 序列化/反序列化是多余的绕路。直接调用 `createGenericAgent` 更简单也更类型安全。**教训：为跨进程设计的 adapter 不一定适合同进程场景。不要因为"已经有了"就复用——检查适配成本。**

3. **Hexagonal pattern 的一致性需要 commit-level review**。Agent、thread、checkpoint 三个 feature 都严格遵循了 domain → ports → adapter → service → http 模式，唯独 run feature 的 port 是 inline 的。这不是设计意图——是 commit 8 实现时赶工的结果。**教训：每 commit 做完后对照上一个 commit 的 feature 结构做一致性检查。**

4. **Migration 版本号的命名空间需要提前约定**。Backend 和 checkpointer-sqlite 各自独立维护 migration，合并到共库时 ID 碰撞。方案（1000+ range）简单有效，但应该在 checkpointer-sqlite 设计阶段就确定，而非 review 发现后才修。**教训：多个包向同一 DB 贡献 migration 时，ID range 分配是跨包接口契约的一部分。**

5. **Spec 测试预估不应成为约束**。Spec 预估 ~104 tests，实际 ~60。差距主要在 HTTP/e2e 层——spec 按"每层每方法一个 test"线性估算，但 HTTP handler 薄到 route → service → response，adapter test 和 service test 已经覆盖了核心逻辑；HTTP test 只需验证 validation + error status mapping。**教训：测试数量预估应该按 risk 分配，不是按方法数乘层数。**

## M9 前 Bug Fix Round（2026-06-06）

M8 code review 后，抽取了必须 M9 之前修掉的 12 项必修 + 4 项顺带清理。一轮修完，321 tests，0 fail，CI 全绿。

### 必修修复（12 项）

| ID | 问题 | 修复 |
|---|---|---|
| H1 | 多 tool_use 中断：第 1 个中断后其余 N-1 个无 tool_result → API 400 | 中断时对同批未执行 call 补 isError tool_result |
| H2 | user_version 设成 maxId=1003，未来 id 7-999 被静默跳过 | 逐 migration bump `PRAGMA user_version`，包在事务里 |
| H3 | `port.create` 抛错 → `threads.add` 泄漏 → 线程永久 409 | 包装 try/catch，失败时 `threads.delete` + `abortMap.delete` |
| H5 | `process.exit` 截断管道缓冲 → 终态事件丢失 | 退出前 await stdout drain（writableLength + drain 事件） |
| H6 | model 构造在 try 外 → 配置错误裸崩 | `new AnthropicChatModel` 移入 try/catch |
| N1 | SSE done 用 inband `payload.message==="done"` 字符串匹配 → error 事件 payload 恰好 "done" 误判 | 删除 inband 检测，改用 `writeSseDone()` 在 for-await 结束后合成；顺手复用 `infra/sse.ts`（清 I4 死代码） |
| M2 | beforeModel/beforeTool 无错误隔离 → 一个 plugin 抛错整轮 abort | 加 try/catch + `logger.warn`，与 after-hook 对齐 |
| M5 | `JSON.parse` 损坏行无保护 → 堆栈泄漏 500 | checkpointer-sqlite 3 处 + backend checkpoint adapter 1 处加 try/catch |
| M6 | handler throw 冒到 Bun 默认 500 泄漏堆栈；错误方法无 405 | router 外层加 try/catch；已知路径捕 405 |
| L2 | token 字符串直接比较 | 换 `crypto.timingSafeEqual` |
| I2 | tablePrefix 非功能参数 → 传值必崩 | 显式 reject 非空 prefix（`throw new Error`） |
| N3 | thread.delete 与 cleanupCheckpoint 无事务 → 孤儿 checkpoint 行 | cleanup 加 try/catch best-effort |

### 顺带清理

| 项 | 修复 |
|---|---|
| flaky mtime-cache test | 写文件后显式 `invalidateSkillCache`，避免 mtime 分辨率竞态 |
| I4 SSE helper dead code | N1 修复中 `run/http.ts` 改调 `infra/sse.ts` 的 `writeSseEvent`/`writeSseDone` |

### 明确推迟到 M9（未动）

H4（客户端断连继续跑）、M4（checkpointer busy_timeout/WAL checkpoint）、M7（run 后命中已删 thread）、I3/I5/S1/S4/M8/M9/L1/L3-L7。

### 本轮教训

6. **before-hook 隔离是"改了行为"的 fix**。M2 把 beforeModel/beforeTool 的 throw 传播改为 warn+继续——两个旧测试的断言（`.rejects.toThrow`）需要同时更新。不是 bug，是行为变更。**教训：改错误传播语义时，先 grep 所有 `.rejects.toThrow` 确认变更范围。**

7. **修复密度越高，越不能跳过 CI**。13 个文件一次修完，如果逐步做每步都 CI 验证会更慢但更安全。一次全修的好处是只跑一轮 CI，风险是哪个改动引入新问题不易定位。M9 前的这种"扫尾 round"规模下全量修+一次 CI 是合理的，但 M9 主线应回归逐 commit CI。

## 与 M6/M7 的对比

| 维度 | M6 | M7 | M8 |
|---|---|---|---|
| 交付类型 | harness + tools 扩充 + CLI | schema + runner + type extension | backend app + checkpointer package + harness 切换 |
| 生产 LOC | ~400 | ~95 | ~970 |
| 测试 LOC | ~578 | ~370 | ~770 |
| 新测试数 | 44 | 28 | 60 |
| 总测试数 | 234 | 262 | 320 |
| 外部依赖 | 0 | 1（zod） | 1（zod） |
| 系统依赖 | 1（ripgrep） | 0 | 0（bun:sqlite built-in） |
| 新增包 | 0 | 2 | 1（+1 app） |
| Grill 决议 | 14 | 16（预 grilled） | 22 |
| TDD 纪律 | 严格遵守 | 后半段严格 | 前半段严格，后半段松动 |
| Review 发现问题 | 19 | 4 | 14 |
| Critical bug | 1（P1） | 0 | **2**（C1 runner、C2 types）|

---

**Retro 结束。**
