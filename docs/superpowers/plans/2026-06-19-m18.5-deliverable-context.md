# M18.5 — 交付物结构化 + Issue 上下文累积 + 模板变量提取 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 打通「planner 提交结构化交付物 → developer 通过 `{{deliverables.plan.summary}}` 取用」的最小闭环——新增 Deliverable 本体 + `submitDeliverable` 工具 + `renderPrompt` 路径取值 + `surfaceContext` 注入通道。

**Architecture:** 独立 `features/deliverable/` 模块（追加式落库）+ `renderPrompt` 正则扩 `[\w.]+` 嵌套路径取值（保持纯插值无 DSL）+ `surfaceContext → extraTools` 通道注入 `submit_deliverable` 工具。Orchestrator 的 `startStep` 是唯一集成点：构建变量字典时读交付物、起 run 时传 `surfaceContext.issue`。

**Tech Stack:** bun:sqlite, zod, TypeScript ESM, JSON Schema (tool inputSchema)

**Baseline:** `027f6a7`

**Decisions:**
- Q1: `idempotency_key TEXT` + 部分唯一索引直接进 5012 CREATE TABLE（一次建表，无 ALTER）
- Q2: `issueRoutes(svc, opsStore, deliverableSvc, opts?)` 必选协作者走位置参数，可选回调走尾部 opts

---

## 文件变更地图

| 文件 | 动作 | 职责 |
|------|------|------|
| `apps/backend/src/infra/sqlite/migrations.ts` | 改 | 追加 5012 `backend_v26_deliverable` |
| `apps/backend/src/features/deliverable/domain.ts` | 新 | `DeliverableRow` 接口 |
| `apps/backend/src/features/deliverable/ports.ts` | 新 | `DeliverablePort` 接口 |
| `apps/backend/src/features/deliverable/adapter-sqlite.ts` | 新 | SQLite 适配器（Raw/toRow + JSON 列） |
| `apps/backend/src/features/deliverable/service.ts` | 新 | `createDeliverableService()` |
| `apps/backend/src/features/deliverable/index.ts` | 新 | 统一导出 barrel |
| `apps/backend/src/features/orchestrator/render.ts` | 改 | regex `[\w.]+` + 嵌套路径 + `PromptVars` 类型 |
| `apps/backend/src/features/orchestrator/render.test.ts` | 改 | 扩充嵌套路径测试 |
| `apps/backend/src/features/orchestrator/reactor.ts` | 改 | `OrchestratorDeps` 加 `deliverableSvc`，`startStep` 构建上下文 + 传 surfaceContext |
| `apps/backend/src/main.ts` | 改 | 构造 `deliverableSvc`，传入 `createOrchestrator` 和 `issueRoutes` |
| `packages/runner-protocol/src/messages.ts` | 改 | `surfaceContext` schema 扩展 |
| `apps/backend/src/features/run/supervisor.ts` | 改 | `RunRequestOptions.surfaceContext` 类型镜像 |
| `packages/runner-daemon/src/submit-deliverable-tool.ts` | 新 | `createSubmitDeliverableTool` 工厂 |
| `packages/runner-daemon/src/runner-daemon.ts` | 改 | 注入 `submit_deliverable` extraTool |
| `apps/backend/src/features/issue/http.ts` | 改 | 签名改 `(svc, opsStore, deliverableSvc, opts?)`，新增 `submitDeliverable` handler |
| `apps/backend/src/http/router.ts` | 改 | issue 块加 `POST /api/issues/:id/deliverables` 路由 |
| `apps/backend/src/features/deliverable/service.test.ts` | 新 | 交付物服务真库测试 |
| `apps/backend/src/features/orchestrator/reactor.test.ts` | 改 | `deliverableSvc` 桩 + surfaceContext 断言 |
| `apps/backend/src/features/issue/http.test.ts` | 改 | 交付物端点测试 |
| `packages/runner-daemon/src/submit-deliverable-tool.test.ts` | 新 | 工具 HTTP 契约测试 |

---

### Task 1: P1 — 交付物表迁移

**Files:**
- Modify: `apps/backend/src/infra/sqlite/migrations.ts` (append after id 5011)

- [ ] **Step 1: 追加 5012 迁移**

在 `BACKEND_MIGRATIONS` 数组末尾（`backend_v25_column_config` 之后）追加。一次性包含 `idempotency_key` 列 + 部分唯一索引。

```ts
  // ─── M18.5 deliverable — structured hand-off artifacts ──
  {
    name: "backend_v26_deliverable",
    id: 5012,
    up: `CREATE TABLE IF NOT EXISTS deliverable (
      deliverable_id   TEXT PRIMARY KEY,
      issue_id         TEXT NOT NULL,
      from_status      TEXT NOT NULL,
      kind             TEXT NOT NULL,
      fields           TEXT NOT NULL,
      ref              TEXT,
      run_id           TEXT,
      idempotency_key  TEXT,
      created_at       INTEGER NOT NULL
    );
    CREATE INDEX IF NOT EXISTS idx_deliverable_issue ON deliverable(issue_id);
    CREATE INDEX IF NOT EXISTS idx_deliverable_issue_kind ON deliverable(issue_id, kind);
    CREATE UNIQUE INDEX IF NOT EXISTS idx_deliverable_idem ON deliverable(idempotency_key) WHERE idempotency_key IS NOT NULL;`,
  },
```

- [ ] **Step 2: 验证迁移可执行**

```bash
cd apps/backend && bun -e "
import { Database } from 'bun:sqlite';
const db = new Database(':memory:');
db.exec('CREATE TABLE IF NOT EXISTS deliverable (deliverable_id TEXT PRIMARY KEY, issue_id TEXT NOT NULL, from_status TEXT NOT NULL, kind TEXT NOT NULL, fields TEXT NOT NULL, ref TEXT, run_id TEXT, idempotency_key TEXT, created_at INTEGER NOT NULL)');
db.exec('CREATE UNIQUE INDEX IF NOT EXISTS idx_deliverable_idem ON deliverable(idempotency_key) WHERE idempotency_key IS NOT NULL');
console.log('OK');
"
```

Expected: `OK`

- [ ] **Step 3: Commit**

```bash
git add apps/backend/src/infra/sqlite/migrations.ts
git commit -m "feat(backend): add deliverable table migration (5012) for M18.5"
```

---

### Task 2: P1 — 交付物 domain + port + adapter + service + index

**Files:**
- Create: `apps/backend/src/features/deliverable/domain.ts`
- Create: `apps/backend/src/features/deliverable/ports.ts`
- Create: `apps/backend/src/features/deliverable/adapter-sqlite.ts`
- Create: `apps/backend/src/features/deliverable/service.ts`
- Create: `apps/backend/src/features/deliverable/index.ts`

- [ ] **Step 1: 写 domain.ts**

```ts
/** 交付物 — 某一棒执行器在干完时显式提交的结构化产物。
 *  追加式落库：同 (issueId, kind) 可多行，取 created_at 最大者为最新。 */
export interface DeliverableRow {
  deliverableId: string;
  issueId: string;
  fromStatus: string;
  kind: string;
  fields: Record<string, string>;
  ref: string | null;
  runId: string | null;
  idempotencyKey: string | null;
  createdAt: number;
}
```

- [ ] **Step 2: 写 ports.ts**

```ts
import type { DeliverableRow } from "./domain.js";

export interface DeliverablePort {
  insert(input: {
    deliverableId: string;
    issueId: string;
    fromStatus: string;
    kind: string;
    fields: Record<string, string>;
    ref: string | null;
    runId: string | null;
    idempotencyKey: string | null;
    createdAt: number;
  }): DeliverableRow;

  /** ORDER BY created_at ASC — 消费端按 kind 聚合时后写覆盖（取最新）。 */
  listByIssue(issueId: string): DeliverableRow[];

  /** 幂等查重。idempotencyKey 可空（未传则不走幂等）。 */
  getByIdempotencyKey(key: string): DeliverableRow | null;
}
```

- [ ] **Step 3: 写 adapter-sqlite.ts**

照 `column-config/adapter-sqlite.ts` 的 Raw/toRow + snake_case→camelCase 模式。`fields` 列 JSON 往返。

```ts
import type { Database } from "bun:sqlite";
import type { DeliverableRow } from "./domain.js";
import type { DeliverablePort } from "./ports.js";

type Raw = {
  deliverable_id: string;
  issue_id: string;
  from_status: string;
  kind: string;
  fields: string;
  ref: string | null;
  run_id: string | null;
  idempotency_key: string | null;
  created_at: number;
};

const toRow = (r: Raw): DeliverableRow => ({
  deliverableId: r.deliverable_id,
  issueId: r.issue_id,
  fromStatus: r.from_status,
  kind: r.kind,
  fields: JSON.parse(r.fields) as Record<string, string>,
  ref: r.ref,
  runId: r.run_id,
  idempotencyKey: r.idempotency_key,
  createdAt: r.created_at,
});

export function sqliteDeliverableAdapter(db: Database): DeliverablePort {
  return {
    insert(input): DeliverableRow {
      db.run(
        `INSERT INTO deliverable (deliverable_id, issue_id, from_status, kind, fields, ref, run_id, idempotency_key, created_at)
         VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?)`,
        [
          input.deliverableId,
          input.issueId,
          input.fromStatus,
          input.kind,
          JSON.stringify(input.fields),
          input.ref,
          input.runId,
          input.idempotencyKey,
          input.createdAt,
        ],
      );
      return {
        deliverableId: input.deliverableId,
        issueId: input.issueId,
        fromStatus: input.fromStatus,
        kind: input.kind,
        fields: input.fields,
        ref: input.ref,
        runId: input.runId,
        idempotencyKey: input.idempotencyKey,
        createdAt: input.createdAt,
      };
    },

    listByIssue(issueId: string): DeliverableRow[] {
      const rows = db
        .query("SELECT * FROM deliverable WHERE issue_id = ? ORDER BY created_at ASC")
        .all(issueId) as Raw[];
      return rows.map(toRow);
    },

    getByIdempotencyKey(key: string): DeliverableRow | null {
      const r = db
        .query("SELECT * FROM deliverable WHERE idempotency_key = ?")
        .get(key) as Raw | undefined;
      return r ? toRow(r) : null;
    },
  };
}
```

- [ ] **Step 4: 写 service.ts**

```ts
import type { DeliverableRow } from "./domain.js";
import type { DeliverablePort } from "./ports.js";

export function createDeliverableService(deps: {
  port: DeliverablePort;
  idGen: () => string;
  now?: () => number;
}) {
  const now = deps.now ?? Date.now;
  return {
    port: deps.port,

    /** 提交一份交付物（追加）。归属 (issueId, fromStatus) 由调用方补全后传入。 */
    submit(input: {
      issueId: string;
      fromStatus: string;
      kind: string;
      fields: Record<string, string>;
      ref?: string;
      runId?: string;
      idempotencyKey?: string;
    }): DeliverableRow {
      return deps.port.insert({
        deliverableId: deps.idGen(),
        issueId: input.issueId,
        fromStatus: input.fromStatus,
        kind: input.kind,
        fields: input.fields,
        ref: input.ref ?? null,
        runId: input.runId ?? null,
        idempotencyKey: input.idempotencyKey ?? null,
        createdAt: now(),
      });
    },

    listByIssue(issueId: string): DeliverableRow[] {
      return deps.port.listByIssue(issueId);
    },
  };
}

export type DeliverableService = ReturnType<typeof createDeliverableService>;
```

- [ ] **Step 5: 写 index.ts (barrel)**

```ts
export { sqliteDeliverableAdapter } from "./adapter-sqlite.js";
export type { DeliverableRow } from "./domain.js";
export type { DeliverablePort } from "./ports.js";
export { createDeliverableService } from "./service.js";
export type { DeliverableService } from "./service.js";
```

- [ ] **Step 6: 验证类型检查**

```bash
cd apps/backend && bun run typecheck
```

Expected: 无新增 type error（可能有未使用导入告警，P3/P7 接线后消除）。

- [ ] **Step 7: Commit**

```bash
git add apps/backend/src/features/deliverable/
git commit -m "feat(backend): add Deliverable feature module for M18.5"
```

---

### Task 3: P2 — `renderPrompt` 升级：路径取值

**Files:**
- Modify: `apps/backend/src/features/orchestrator/render.ts`

- [ ] **Step 1: 改 render.ts**

```ts
/** Only {{path}} string interpolation. Supports nested dot-path lookup
 *  (e.g. {{deliverables.plan.summary}}). Missing paths fall back to empty string.
 *  Non-string leaf values (intermediate objects) also fall back to empty string.
 *  Deliberately no conditionals, loops, or filters — no template DSL. */
export type PromptVars = { [k: string]: string | PromptVars };

export function renderPrompt(template: string, vars: PromptVars): string {
  return template.replace(/\{\{([\w.]+)\}\}/g, (_, path: string) => {
    const v = path.split(".").reduce<unknown>(
      (acc, seg) =>
        acc != null && typeof acc === "object" ? (acc as Record<string, unknown>)[seg] : undefined,
      vars,
    );
    return typeof v === "string" ? v : "";
  });
}
```

- [ ] **Step 2: 更新 render.test.ts 的 vars 类型**

`render.test.ts:6-9` 的 `renderPrompt` 调用 `vars` 参数类型从 `Record<string, string>` 变为 `PromptVars`。由于 `Record<string, string>` 是 `PromptVars` 的子类型，现有测试无需修改——但需确认。

```bash
cd apps/backend && bun test --test-name-pattern="renderPrompt"
```

Expected: 4 tests pass（现有单层插值测试照旧通过）。

- [ ] **Step 3: Commit**

```bash
git add apps/backend/src/features/orchestrator/render.ts
git commit -m "feat(backend): upgrade renderPrompt to support dot-path nested lookup"
```

---

### Task 4: P3 — `startStep` 构建累积上下文 + 注入 surfaceContext

**Files:**
- Modify: `apps/backend/src/features/orchestrator/reactor.ts`
- Modify: `apps/backend/src/main.ts`

- [ ] **Step 1: 更新 OrchestratorDeps + startStep（reactor.ts）**

`OrchestratorDeps` 新增 `deliverableSvc`；`startStep` 构建嵌套变量字典 + 传第四参 surfaceContext。

```ts
import type { DeliverableRow } from "../deliverable/domain.js";

// 在 OrchestratorDeps 接口中新增：
export interface OrchestratorDeps {
  // ... 现有字段不变
  deliverableSvc: { listByIssue(issueId: string): DeliverableRow[] };
  // ...
}

// 在 createOrchestrator 函数体内解构：
const { issueSvc, agentSvc, supervisor, opsStore, buildSpec, idGen, columnConfigSvc, deliverableSvc } = deps;

// 新增 buildPromptVars（放在 createOrchestrator 内、startStep 外）：
function buildPromptVars(issue: IssueRow, deliverables: DeliverableRow[]): PromptVars {
  const byKind: Record<string, Record<string, string>> = {};
  for (const d of deliverables) {
    byKind[d.kind] = { ...d.fields, ...(d.ref ? { ref: d.ref } : {}) };
  }
  return { title: issue.title, issueId: issue.issueId, deliverables: byKind };
}

// 修改 startStep 的 renderPrompt 调用和 startMainRun 调用：
async function startStep(issue: IssueRow): Promise<{ runId: string } | null> {
  const table = columnConfigSvc.transitionsForProject(issue.projectId);
  const t = nextTransition(table, issue.status);
  if (!t) return null;

  const agent = await agentSvc.getById(t.agentId).catch(() => null);
  if (!agent) {
    throw new OrchestratorAgentMissingError(t.agentId, issue.issueId);
  }

  const runId = idGen();
  const vars = buildPromptVars(issue, deliverableSvc.listByIssue(issue.issueId));
  const prompt = renderPrompt(t.promptTemplate, vars);
  const spec = await buildSpec(t.agentId, issue.threadId, prompt);

  await supervisor.startMainRun(runId, issue.threadId, spec, {
    surfaceContext: {
      surface: "orchestrator",
      conversationId: "",
      runId,
      capabilities: ["submit_deliverable"],
      issue: { issueId: issue.issueId, fromStatus: issue.status },
    },
  });

  opsStore.insertRunOrigin({
    runId,
    issueId: issue.issueId,
    conversationId: "",
    sourceLedgerSeq: 0,
    agentMemberId: t.agentId,
    surface: "orchestrator",
    traceId: "",
    traceparent: "",
    idempotencyKey: `issue:${issue.issueId}:${issue.status}:run`,
    createdAt: (deps.now ?? Date.now)(),
  });

  return { runId };
}
```

导入更新：
```ts
import type { DeliverableRow } from "../deliverable/domain.js";
import { renderPrompt } from "./render.js";        // 路径不变
import type { PromptVars } from "./render.js";      // 新增导入
```

- [ ] **Step 2: main.ts 接线 deliverableSvc**

在 `main.ts` 中：
1. 导入 `createDeliverableService` 和 `sqliteDeliverableAdapter`
2. 在 `columnConfigSvc` 之后构造 `deliverableSvc`
3. 传入 `createOrchestrator`
4. 传入 `issueRoutes`

```ts
// 导入区新增：
import {
  createDeliverableService,
  sqliteDeliverableAdapter,
} from "./features/deliverable/index.js";

// columnConfigSvc 之后新增：
const deliverableSvc = createDeliverableService({
  port: sqliteDeliverableAdapter(db),
  idGen: ulid,
});

// createOrchestrator 调用新增 deliverableSvc：
const orchestrator = createOrchestrator({
  issueSvc,
  agentSvc,
  supervisor,
  opsStore,
  buildSpec: buildIssueSpec,
  idGen: ulid,
  columnConfigSvc,
  deliverableSvc,                              // ← 新增
});

// issueRoutes 调用签改：
issues: issueRoutes(issueSvc, opsStore, deliverableSvc, {
  onIssueStarted: (issue) => orchestrator.startStep(issue),
}),
```

- [ ] **Step 3: 验证类型检查**

```bash
cd apps/backend && bun run typecheck
```

Expected: 无新增 type error。若 `issueRoutes` 签名尚未改为新签，会有 type error——这是预期的，P7 会修。

- [ ] **Step 4: Commit**

```bash
git add apps/backend/src/features/orchestrator/reactor.ts apps/backend/src/main.ts
git commit -m "feat(backend): build accumulated issue context and inject surfaceContext in startStep"
```

---

### Task 5: P4 — `surfaceContext` schema 扩展

**Files:**
- Modify: `packages/runner-protocol/src/messages.ts:45-52`
- Modify: `apps/backend/src/features/run/supervisor.ts:42-47`

- [ ] **Step 1: 改 protocol messages.ts**

```ts
surfaceContext: z
  .object({
    surface: z.enum(["lark", "web", "cli", "orchestrator"]),
    conversationId: z.string(),
    runId: z.string(),
    capabilities: z.array(z.enum(["start_new_conversation", "submit_deliverable"])),
    issue: z.object({ issueId: z.string(), fromStatus: z.string() }).optional(),
  })
  .optional(),
```

- [ ] **Step 2: 改 supervisor.ts 镜像类型**

```ts
surfaceContext?: {
  surface: "lark" | "web" | "cli" | "orchestrator";
  conversationId: string;
  runId: string;
  capabilities: Array<"start_new_conversation" | "submit_deliverable">;
  issue?: { issueId: string; fromStatus: string };
};
```

- [ ] **Step 3: 确认类型检查**

```bash
bun run typecheck   # 全仓
```

Expected: 无新增 type error。`capabilities` 从 `z.literal` 改为 `z.enum` 不影响既有 `sc.capabilities.includes("start_new_conversation")` 调用。

- [ ] **Step 4: Commit**

```bash
git add packages/runner-protocol/src/messages.ts apps/backend/src/features/run/supervisor.ts
git commit -m "feat: extend surfaceContext schema with orchestrator surface, submit_deliverable capability, and issue field"
```

---

### Task 6: P5 — `createSubmitDeliverableTool`

**Files:**
- Create: `packages/runner-daemon/src/submit-deliverable-tool.ts`

- [ ] **Step 1: 写工具文件**

```ts
import type { Tool } from "@my-agent-team/core";

/**
 * M18.5: Create a `submit_deliverable` tool for issue runs.
 * The tool POSTs structured deliverables to the backend's
 * POST /api/issues/:issueId/deliverables endpoint.
 *
 * Unlike start_new_conversation (which throws on failure), this tool
 * returns { isError: true } — deliverable submission is best-effort
 * and should not crash the entire run.
 */
export function createSubmitDeliverableTool(input: {
  backendUrl: string;
  backendAuthToken: string | null;
  issueId: string;
  fromStatus: string;
  runId: string;
}): Tool {
  return {
    name: "submit_deliverable",
    description:
      "Submit your structured deliverable for the current issue step before finishing. " +
      "Put small extractable values in `fields` (e.g. plan summary, MR url/title, review verdict); " +
      "put large artifacts behind `ref` (a doc/MR link), not inline.",
    inputSchema: {
      type: "object",
      properties: {
        kind: {
          type: "string",
          description: "Deliverable type, e.g. plan | mr | review.",
        },
        fields: {
          type: "object",
          additionalProperties: { type: "string" },
          description: "Structured small fields for the next step to extract.",
        },
        ref: {
          type: "string",
          description: "Pointer to a large artifact (doc/MR link). Optional.",
        },
      },
      required: ["kind", "fields"],
    },
    async execute(args: unknown) {
      const a = args as { kind: string; fields: Record<string, string>; ref?: string };
      const url = `${input.backendUrl}/api/issues/${input.issueId}/deliverables`;
      const headers: Record<string, string> = { "Content-Type": "application/json" };
      if (input.backendAuthToken) headers["x-auth-token"] = input.backendAuthToken;
      const resp = await fetch(url, {
        method: "POST",
        headers,
        body: JSON.stringify({
          kind: a.kind,
          fields: a.fields,
          ref: a.ref,
          fromStatus: input.fromStatus,
          runId: input.runId,
          idempotencyKey: `issue:${input.issueId}:${input.fromStatus}:deliverable`,
        }),
      });
      if (!resp.ok) {
        const body = (await resp.json().catch(() => ({}))) as { error?: string };
        return {
          content: `submit_deliverable failed: HTTP ${resp.status} - ${body.error ?? "unknown"}`,
          isError: true,
        };
      }
      return { content: await resp.text() };
    },
  };
}
```

- [ ] **Step 2: 确认类型检查**

```bash
cd packages/runner-daemon && bun run typecheck
```

Expected: 无 type error（可能警告未使用导出——P6 消除）。

- [ ] **Step 3: Commit**

```bash
git add packages/runner-daemon/src/submit-deliverable-tool.ts
git commit -m "feat(runner-daemon): add createSubmitDeliverableTool for M18.5"
```

---

### Task 7: P6 — runner-daemon 注入分支

**Files:**
- Modify: `packages/runner-daemon/src/runner-daemon.ts:178-194`

- [ ] **Step 1: 加 import + 注入分支**

`runner-daemon.ts` 顶部 import 区：
```ts
import { createSubmitDeliverableTool } from "./submit-deliverable-tool.js";
```

在 `extraTools` 段（lark 分支之后、`createGenericAgent` 之前）追加：
```ts
    // M18.5: Inject submit_deliverable tool for issue runs.
    // Condition: capability present + issue info attached + not a reflect run.
    // We don't bind on surface literal — the capability+issue combo is the signal.
    if (
      sc?.capabilities.includes("submit_deliverable") &&
      sc.issue &&
      spec.mode !== "reflect"
    ) {
      extraTools.push(
        createSubmitDeliverableTool({
          backendUrl: this.#backendUrl,
          backendAuthToken: this.#backendAuthToken,
          issueId: sc.issue.issueId,
          fromStatus: sc.issue.fromStatus,
          runId: sc.runId,
        }),
      );
    }
```

> 注意 TypeScript 类型缩窄：`sc.capabilities` 在 P4 前类型是 `Array<"start_new_conversation">`，P4 后是 `Array<"start_new_conversation" | "submit_deliverable">`。`sc.issue` 是 P4 新增的可选字段。需 P4 已落才能通过 typecheck。

- [ ] **Step 2: 确认类型检查**

```bash
cd packages/runner-daemon && bun run typecheck
```

Expected: 无 type error。

- [ ] **Step 3: Commit**

```bash
git add packages/runner-daemon/src/runner-daemon.ts
git commit -m "feat(runner-daemon): inject submit_deliverable tool for issue runs"
```

---

### Task 8: P7 — 交付物回调端点 + 路由接线

**Files:**
- Modify: `apps/backend/src/features/issue/http.ts`
- Modify: `apps/backend/src/http/router.ts`
- Modify: `apps/backend/src/main.ts`（已在 Task 4 Step 2 改了调用点，此处只确认）

- [ ] **Step 1: 改 issueRoutes 签名 + 新增 submitDeliverable handler（http.ts）**

签名从 `issueRoutes(svc, opts?)` 改为 `issueRoutes(svc, opsStore, deliverableSvc, opts?)`。

新增 `deliverableSchema` 和 `submitDeliverable` handler，在返回对象中放到 `transition` 之后、`meta` 之前。

```ts
import { z } from "zod";
import { json, sseResponse } from "../../http/response.js";
import { ISSUE_STATUSES, ORDER } from "../orchestrator/transitions.js";
import type { IssueRow, IssueStatus } from "./entities.js";
import {
  IllegalTransitionError,
  IssueNotFoundError,
  type IssueService,
  ValidationError,
} from "./service.js";
import type { RuntimeOpsStore } from "../runtime-ops/store.js";
import type { DeliverableService } from "../deliverable/service.js";

// ... 现有 createSchema, transitionSchema 不变 ...

const deliverableSchema = z.object({
  kind: z.string().trim().min(1),
  fields: z.record(z.string()),
  ref: z.string().optional(),
  fromStatus: z.string().optional(),
  runId: z.string().optional(),
  idempotencyKey: z.string().optional(),
});

export function issueRoutes(
  svc: IssueService,
  opsStore: RuntimeOpsStore,
  deliverableSvc: DeliverableService,
  opts?: { onIssueStarted?: (issue: IssueRow) => Promise<unknown> },
) {
  const { onIssueStarted } = opts ?? {};

  return {
    // ... 现有 create, list, get, transition, meta, events 不变 ...

    /** POST /api/issues/:id/deliverables → 201 { deliverable } | 404 | 409 */
    async submitDeliverable(req: Request, issueId: string): Promise<Response> {
      const parsed = deliverableSchema.safeParse(await req.json().catch(() => ({})));
      if (!parsed.success)
        return json({ error: "Validation failed", details: parsed.error.issues }, 400);
      if (!svc.port.getIssue(issueId)) return json({ error: "issue not found" }, 404);

      let fromStatus = parsed.data.fromStatus ?? "";
      if (parsed.data.runId) {
        const origin = opsStore.getRunOrigin(parsed.data.runId);
        if (origin?.issueId && origin.issueId !== issueId)
          return json({ error: "run/issue mismatch" }, 409);
        fromStatus = origin?.idempotencyKey?.split(":")[2] ?? fromStatus;
      }

      const key = parsed.data.idempotencyKey;
      const existing = key ? deliverableSvc.port.getByIdempotencyKey(key) : null;
      if (existing) return json({ deliverable: existing });

      const row = deliverableSvc.submit({
        issueId,
        fromStatus,
        kind: parsed.data.kind,
        fields: parsed.data.fields,
        ref: parsed.data.ref,
        runId: parsed.data.runId,
        idempotencyKey: key,
      });
      return json({ deliverable: row }, 201);
    },
  };
}
```

- [ ] **Step 2: 加路由（router.ts）**

在 issue 块内，`issueEventsMatch` 之后、`issueTransitionMatch` 之前插入：

```ts
      // M18.5: Deliverable submission — must be before /:id detail regex
      if (issues) {
        // ... 现有 issuesListMatch, issueEventsMatch ...

        const issueDeliverablesMatch = path.match(/^\/api\/issues\/([^/]+)\/deliverables$/);

        // ... issueEventsMatch + issueMetaMatch + issuesListMatch 处理 ...
        // 在 issuesListMatch 405 之后、issueTransitionMatch 之前插入：

        if (issueDeliverablesMatch && method === "POST")
          return withAuth(
            async (r) => issues.submitDeliverable(r, issueDeliverablesMatch[1]!),
            token,
          )(req);

        // ... 后面 issueTransitionMatch, issueDetailMatch 不变 ...
```

具体插入位置：紧跟 `if (issuesListMatch) return json({ error: "Method not allowed" }, 405);` 之后。

并在末尾 405 区追加：
```ts
        if (issueDeliverablesMatch) return json({ error: "Method not allowed" }, 405);
```

- [ ] **Step 3: main.ts 调用点确认**

Task 4 Step 2 已将 `issueRoutes` 调用改为新签名。此处确认一致：
```ts
issues: issueRoutes(issueSvc, opsStore, deliverableSvc, {
  onIssueStarted: (issue) => orchestrator.startStep(issue),
}),
```

- [ ] **Step 4: 确认类型检查**

```bash
cd apps/backend && bun run typecheck
```

Expected: 无 type error。

- [ ] **Step 5: Commit**

```bash
git add apps/backend/src/features/issue/http.ts apps/backend/src/http/router.ts
git commit -m "feat(backend): add POST /api/issues/:id/deliverables endpoint and routing"
```

---

### Task 9: P8 — 交付物服务测试

**Files:**
- Create: `apps/backend/src/features/deliverable/service.test.ts`

- [ ] **Step 1: 写测试**

真库测试（`:memory:` + migration），仿 `issue/service.test.ts` 模式。

```ts
import { Database } from "bun:sqlite";
import { describe, expect, test, beforeAll } from "bun:test";
import { createDeliverableService } from "./service.js";
import { sqliteDeliverableAdapter } from "./adapter-sqlite.js";

function setup() {
  const db = new Database(":memory:");
  db.exec(`CREATE TABLE IF NOT EXISTS deliverable (
    deliverable_id TEXT PRIMARY KEY,
    issue_id       TEXT NOT NULL,
    from_status    TEXT NOT NULL,
    kind           TEXT NOT NULL,
    fields         TEXT NOT NULL,
    ref            TEXT,
    run_id         TEXT,
    idempotency_key TEXT,
    created_at     INTEGER NOT NULL
  );
  CREATE UNIQUE INDEX IF NOT EXISTS idx_deliverable_idem ON deliverable(idempotency_key) WHERE idempotency_key IS NOT NULL`);
  let idCounter = 0;
  const svc = createDeliverableService({
    port: sqliteDeliverableAdapter(db),
    idGen: () => `d_${String(++idCounter).padStart(3, "0")}`,
    now: () => 1000 + idCounter,
  });
  return { db, svc };
}

describe("DeliverableService", () => {
  test("submit inserts a row and returns it", () => {
    const { svc } = setup();
    const row = svc.submit({
      issueId: "iss_001",
      fromStatus: "planned",
      kind: "plan",
      fields: { summary: "Build a thing", url: "https://doc.example/plan" },
      ref: "https://doc.example/plan",
      runId: "run_001",
      idempotencyKey: "issue:iss_001:planned:deliverable",
    });
    expect(row.issueId).toBe("iss_001");
    expect(row.fromStatus).toBe("planned");
    expect(row.kind).toBe("plan");
    expect(row.fields).toEqual({ summary: "Build a thing", url: "https://doc.example/plan" });
    expect(row.ref).toBe("https://doc.example/plan");
    expect(row.runId).toBe("run_001");
  });

  test("listByIssue returns rows ordered by created_at ASC", () => {
    const { svc } = setup();
    svc.submit({ issueId: "iss_001", fromStatus: "planned", kind: "plan", fields: { v: "1" } });
    svc.submit({ issueId: "iss_001", fromStatus: "in_progress", kind: "mr", fields: { v: "2" } });
    svc.submit({ issueId: "iss_002", fromStatus: "planned", kind: "plan", fields: { v: "3" } });

    const rows = svc.listByIssue("iss_001");
    expect(rows).toHaveLength(2);
    expect(rows[0]!.kind).toBe("plan");
    expect(rows[1]!.kind).toBe("mr");
    // createdAt 递增（idCounter 递增）
    expect(rows[0]!.createdAt).toBeLessThan(rows[1]!.createdAt);
  });

  test("same (issueId, kind) can have multiple rows (append-only)", () => {
    const { svc } = setup();
    svc.submit({ issueId: "iss_001", fromStatus: "planned", kind: "plan", fields: { v: "1" } });
    svc.submit({ issueId: "iss_001", fromStatus: "planned", kind: "plan", fields: { v: "2" } });

    const rows = svc.port.listByIssue("iss_001");
    expect(rows).toHaveLength(2);
    // 最新（created_at 更大）在后
    expect(rows[1]!.fields).toEqual({ v: "2" });
  });

  test("getByIdempotencyKey finds existing row", () => {
    const { svc } = setup();
    svc.submit({
      issueId: "iss_001",
      fromStatus: "planned",
      kind: "plan",
      fields: { v: "1" },
      idempotencyKey: "key_abc",
    });

    const found = svc.port.getByIdempotencyKey("key_abc");
    expect(found).not.toBeNull();
    expect(found!.issueId).toBe("iss_001");
  });

  test("getByIdempotencyKey returns null for unknown key", () => {
    const { svc } = setup();
    expect(svc.port.getByIdempotencyKey("nonexistent")).toBeNull();
  });

  test("fields JSON round-trips correctly", () => {
    const { svc } = setup();
    const row = svc.submit({
      issueId: "iss_001",
      fromStatus: "planned",
      kind: "plan",
      fields: { summary: "hello", nested: "{\"key\":\"val\"}" },
    });
    const rows = svc.port.listByIssue("iss_001");
    expect(rows[0]!.fields).toEqual({ summary: "hello", nested: '{"key":"val"}' });
  });
});
```

- [ ] **Step 2: 运行测试**

```bash
cd apps/backend && bun test --test-name-pattern="DeliverableService"
```

Expected: 6 tests pass.

- [ ] **Step 3: Commit**

```bash
git add apps/backend/src/features/deliverable/service.test.ts
git commit -m "test(backend): add DeliverableService tests for M18.5"
```

---

### Task 10: P8 — renderPrompt 嵌套路径测试

**Files:**
- Modify: `apps/backend/src/features/orchestrator/render.test.ts`

- [ ] **Step 1: 扩充测试**

在现有 `describe("renderPrompt", ...)` 块内追加测试：

```ts
  test("nested dot-path lookup: {{deliverables.plan.summary}}", () => {
    const vars = {
      title: "Test Issue",
      deliverables: {
        plan: { summary: "Build feature X", ref: "https://doc.example/plan" },
        mr: { url: "https://git.example/mr/1" },
      },
    };
    const result = renderPrompt(
      "Plan: {{deliverables.plan.summary}}, MR: {{deliverables.mr.url}}",
      vars,
    );
    expect(result).toBe("Plan: Build feature X, MR: https://git.example/mr/1");
  });

  test("missing nested path resolves to empty string", () => {
    const vars = {
      title: "Test",
      deliverables: { plan: { summary: "X" } },
    };
    const result = renderPrompt("MR: {{deliverables.mr.url}}", vars);
    expect(result).toBe("MR: ");
  });

  test("intermediate object (non-string leaf) resolves to empty string", () => {
    const vars = {
      deliverables: { plan: { summary: "X" } },
    };
    // {{deliverables.plan}} points to { summary: "X" }, not a string → ""
    const result = renderPrompt("{{deliverables.plan}}", vars);
    expect(result).toBe("");
  });

  test("top-level missing key resolves to empty string", () => {
    const result = renderPrompt("{{nonexistent}}", { title: "T" });
    expect(result).toBe("");
  });

  test("spaces inside braces still not matched (regression)", () => {
    const result = renderPrompt("{{ x }}", { x: "y" });
    expect(result).toBe("{{ x }}");
  });

  test("single-level keys still work (regression)", () => {
    const result = renderPrompt("Hello {{name}}, task: {{task}}", {
      name: "Alice",
      task: "review",
    });
    expect(result).toBe("Hello Alice, task: review");
  });
```

- [ ] **Step 2: 运行测试**

```bash
cd apps/backend && bun test --test-name-pattern="renderPrompt"
```

Expected: 10 tests pass（4 old + 6 new）。

- [ ] **Step 3: Commit**

```bash
git add apps/backend/src/features/orchestrator/render.test.ts
git commit -m "test(backend): add nested path lookup tests for renderPrompt"
```

---

### Task 11: P8 — reactor startStep 上下文 + surfaceContext 测试

**Files:**
- Modify: `apps/backend/src/features/orchestrator/reactor.test.ts`

- [ ] **Step 1: 扩充测试**

在现有测试中：
1. 给 mock deps 加 `deliverableSvc` 桩
2. 新增测试：`startStep` 用历棒交付物渲染 prompt
3. 新增测试：`startMainRun` 收到正确的 `surfaceContext`

```ts
// 在 mock 工厂中新增：
function mockDeliverableSvc(rows: Array<{ kind: string; fields: Record<string, string>; ref?: string; createdAt: number }> = []) {
  return {
    listByIssue(_issueId: string) {
      return rows.map((r, i) => ({
        deliverableId: `d_${i}`,
        issueId: "iss_001",
        fromStatus: "planned",
        kind: r.kind,
        fields: r.fields,
        ref: r.ref ?? null,
        runId: `run_00${i}`,
        idempotencyKey: null,
        createdAt: r.createdAt,
      }));
    },
  };
}

// 新增测试：
test("startStep renders prompt with deliverable fields", async () => {
  let capturedPrompt = "";
  const buildSpec = async (_agentId: string, _threadId: string, input: string) => {
    capturedPrompt = input;
    return { schemaVersion: "2", agentId: "agt_1", threadId: "issue:iss_001", runId: "run_001", mode: "run", input, model: { provider: "anthropic", model: "claude" }, permissionMode: "ask" };
  };

  const deliverableSvc = mockDeliverableSvc([
    { kind: "plan", fields: { summary: "Build login" }, ref: "https://doc.example/plan", createdAt: 1000 },
  ]);

  const orch = createOrchestrator({
    ...baseDeps(),
    buildSpec,
    deliverableSvc,
    columnConfigSvc: mockColumnConfigSvc([{ from: "planned", to: "in_progress", agentId: "agt_1", promptTemplate: "Task: {{deliverables.plan.summary}}, Ref: {{deliverables.plan.ref}}" }]),
  });

  await orch.startStep({ issueId: "iss_001", projectId: "prj_1", title: "Login", status: "planned", threadId: "issue:iss_001", createdAt: 1000, updatedAt: 1000 });
  expect(capturedPrompt).toBe("Task: Build login, Ref: https://doc.example/plan");
});

test("startStep passes surfaceContext to startMainRun", async () => {
  let capturedOpts: any = null;
  const supervisor = {
    ...baseSupervisor(),
    startMainRun: async (_rid: string, _tid: string, _spec: any, opts: any) => {
      capturedOpts = opts;
      return { runId: _rid, attemptId: "att_1" };
    },
  };

  const orch = createOrchestrator({
    ...baseDeps(),
    supervisor,
    deliverableSvc: mockDeliverableSvc([]),
    columnConfigSvc: mockColumnConfigSvc([{ from: "planned", to: "in_progress", agentId: "agt_1", promptTemplate: "Hello" }]),
  });

  await orch.startStep({ issueId: "iss_001", projectId: "prj_1", title: "Login", status: "planned", threadId: "issue:iss_001", createdAt: 1000, updatedAt: 1000 });

  expect(capturedOpts.surfaceContext.surface).toBe("orchestrator");
  expect(capturedOpts.surfaceContext.capabilities).toContain("submit_deliverable");
  expect(capturedOpts.surfaceContext.issue).toEqual({ issueId: "iss_001", fromStatus: "planned" });
});

test("startStep with no prior deliverables renders vars as empty strings", async () => {
  let capturedPrompt = "";
  const buildSpec = async (_agentId: string, _threadId: string, input: string) => {
    capturedPrompt = input;
    return { schemaVersion: "2", agentId: "agt_1", threadId: "issue:iss_001", runId: "run_001", mode: "run", input, model: { provider: "anthropic", model: "claude" }, permissionMode: "ask" };
  };

  const orch = createOrchestrator({
    ...baseDeps(),
    buildSpec,
    deliverableSvc: mockDeliverableSvc([]),
    columnConfigSvc: mockColumnConfigSvc([{ from: "planned", to: "in_progress", agentId: "agt_1", promptTemplate: "Plan: {{deliverables.plan.summary}}" }]),
  });

  await orch.startStep({ issueId: "iss_001", projectId: "prj_1", title: "Login", status: "planned", threadId: "issue:iss_001", createdAt: 1000, updatedAt: 1000 });
  expect(capturedPrompt).toBe("Plan: ");
});
```

- [ ] **Step 2: 运行测试**

```bash
cd apps/backend && bun test --test-name-pattern="reactor"
```

Expected: 新测试 pass，既有测试不回归。

- [ ] **Step 3: Commit**

```bash
git add apps/backend/src/features/orchestrator/reactor.test.ts
git commit -m "test(backend): add startStep context accumulation and surfaceContext tests"
```

---

### Task 12: P8 — 交付物端点 + submit-deliverable-tool 测试

**Files:**
- Modify: `apps/backend/src/features/issue/http.test.ts`（扩充）
- Create: `packages/runner-daemon/src/submit-deliverable-tool.test.ts`

- [ ] **Step 1: 写 http.test.ts 扩充**

新增 `submitDeliverable` 端点测试：

```ts
// 新增 describe 块或追加到现有 describe：
describe("POST /api/issues/:id/deliverables", () => {
  // 需要构造带 opsStore + deliverableSvc 的 issueRoutes
  // 仿现有测试的 setup 模式，新增 deliverable table + adapter
});
```

（具体测试用例：201 落库、404 issue 不存在、409 run/issue mismatch、幂等重复提交、400 validation、路由顺序不被 detail 正则误捕获）

- [ ] **Step 2: 写 submit-deliverable-tool.test.ts**

```ts
import { describe, expect, test, mock } from "bun:test";
import { createSubmitDeliverableTool } from "./submit-deliverable-tool.js";

describe("createSubmitDeliverableTool", () => {
  test("execute sends correct URL, headers, and body", async () => {
    const fetches: Request[] = [];
    globalThis.fetch = mock(async (input: string, init?: RequestInit) => {
      fetches.push(new Request(input, init));
      return new Response('{"deliverable":{"deliverableId":"d_001"}}', { status: 201 });
    });

    const tool = createSubmitDeliverableTool({
      backendUrl: "http://localhost:3000",
      backendAuthToken: "tok_123",
      issueId: "iss_001",
      fromStatus: "planned",
      runId: "run_001",
    });

    const result = await tool.execute({
      kind: "plan",
      fields: { summary: "Build login" },
      ref: "https://doc.example/plan",
    });

    expect(fetches).toHaveLength(1);
    expect(fetches[0]!.url).toBe("http://localhost:3000/api/issues/iss_001/deliverables");
    expect(fetches[0]!.method).toBe("POST");
    expect(fetches[0]!.headers.get("x-auth-token")).toBe("tok_123");

    const body = await fetches[0]!.json();
    expect(body.kind).toBe("plan");
    expect(body.fields).toEqual({ summary: "Build login" });
    expect(body.ref).toBe("https://doc.example/plan");
    expect(body.fromStatus).toBe("planned");
    expect(body.runId).toBe("run_001");
    expect(body.idempotencyKey).toBe("issue:iss_001:planned:deliverable");
  });

  test("non-2xx returns isError:true (does not throw)", async () => {
    globalThis.fetch = mock(async () => new Response('{"error":"not found"}', { status: 404 }));

    const tool = createSubmitDeliverableTool({
      backendUrl: "http://localhost:3000",
      backendAuthToken: null,
      issueId: "iss_001",
      fromStatus: "planned",
      runId: "run_001",
    });

    const result = await tool.execute({ kind: "plan", fields: {} });
    expect(result.isError).toBe(true);
    expect(result.content).toContain("HTTP 404");
  });
});
```

- [ ] **Step 3: 运行测试**

```bash
cd apps/backend && bun test --test-name-pattern="deliverables"
cd packages/runner-daemon && bun test --test-name-pattern="submitDeliverable"
```

Expected: 全部 pass。

- [ ] **Step 4: Commit**

```bash
git add apps/backend/src/features/issue/http.test.ts packages/runner-daemon/src/submit-deliverable-tool.test.ts
git commit -m "test: add deliverable endpoint and submit-deliverable-tool tests for M18.5"
```

---

### Task 13: 终验 — 全量测试 + lint + typecheck

- [ ] **Step 1: 全量后端测试**

```bash
cd apps/backend && bun test
```

Expected: 全部 pass（含既有 270 测试无回归）。

- [ ] **Step 2: 全仓 typecheck + lint**

```bash
bun run typecheck
bun run lint
```

Expected: 18/18 包 typecheck 通过，lint 通过。

- [ ] **Step 3: Commit（若有修复）**

```bash
git add -A
git commit -m "chore: final lint/typecheck fixes for M18.5"
```
