# PR9: Session Context Compaction — Implementation Plan

> 7 commits, each independently passing tsc + tests + arch

---

## pr9-1: compactHistory pure function + tests

### Create `src/domain/compact-history.ts`

```ts
import type { HistoryRecordV1 } from '../application/contracts'

export interface CompactInput {
  history: HistoryRecordV1[]
  summary: string
  keepRecent: number
  sessionId: string
  now?: number
}

export interface CompactOutput {
  newHistory: HistoryRecordV1[]
  removedCount: number
  summaryRecord: HistoryRecordV1 | null
}

export function compactHistory(input: CompactInput): CompactOutput {
  const { history, summary, keepRecent, sessionId } = input
  const now = input.now ?? Date.now()
  if (history.length <= keepRecent) {
    return { newHistory: history, removedCount: 0, summaryRecord: null }
  }
  const cutoff = history.length - keepRecent
  const olderSlice = history.slice(0, cutoff)
  const recent = history.slice(cutoff)
  const summaryRecord: HistoryRecordV1 = {
    kind: 'history.record', version: 1, sessionId,
    role: 'system',
    content: `[Prior conversation summary (${olderSlice.length} msgs compacted)]\n${summary}`,
    ts: now,
    metadata: {
      synthetic: 'compact',
      removedCount: olderSlice.length,
      sourceRange: [olderSlice[0].ts, olderSlice[olderSlice.length - 1].ts],
    },
  }
  return {
    newHistory: [summaryRecord, ...recent],
    removedCount: olderSlice.length,
    summaryRecord,
  }
}
```

### Create `tests/domain/compact-history.test.ts`
7 test cases: below threshold, at threshold+1, summary record fields, sourceRange, deterministic ts, output length verification, tail identity.

### Verify
```bash
bun test tests/domain/compact-history.test.ts     # 7 pass
bun run check:guard                                # 0 errors
```

---

## pr9-2: SessionHistoryPort + replace()

### Files
- Create `src/application/ports/session-history.ts`
- Modify `src/application/usecases/run-turn.ts` — replace inline MessageStorePort with import
- Modify `src/extensions/session/index.ts` — add `replace` to history capability

### Verify
```bash
bun run check:guard
bun test tests/extensions/session-history-replace.test.ts
```

---

## pr9-3: session.compacted event

### Files
- Modify `src/application/contracts/session-events.ts` — add SessionCompactedV1
- Modify `src/application/contracts/events/contracted-event-map.ts` — register event

### Verify
```bash
bun run check:guard
```

---

## pr9-4: compactSessionUsecase

### Files
- Move `src/extensions/session/compact-policy.ts` → `src/application/constants/compact.ts`
- Create `src/application/usecases/compact-session.ts`
- Create `tests/application/compact-session.test.ts`

### Verify
```bash
bun test tests/application/compact-session.test.ts
bun run check:guard
```

---

## pr9-5: LLM compactor + session ext changes

### Files
- Modify `src/extensions/session/compactor.ts` — createCompactor using provider.invoke
- Modify `src/extensions/session/index.ts` — provide compactor capability
- Modify `src/application/constants/compact.ts` — add prompt + constants

### Verify
```bash
bun test
bun run check:guard
```

---

## pr9-6: Wire RPC + slash

### Files
- Modify `src/extensions/controlplane/methods.ts` — wire session.compact
- Modify `src/extensions/frontend.tui/slash/builtin/slash-compact.ts` — use RPC
- Create `tests/extensions/controlplane-session-compact.test.ts`

### Verify
```bash
bun test tests/extensions/controlplane-session-compact.test.ts
bun run check:guard
```

---

## pr9-7: Auto-compact safety net

### Files
- Modify `src/application/usecases/run-turn.ts` — pre-flight check before Phase 2

### Verify
```bash
bun run check:guard
bun test
```
