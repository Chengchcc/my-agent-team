# M21 CronJob Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 落地 CronJob——「一条按时间表反复触发 Agent 运行的定时规则」——后端真实能力 + 前端可 CRUD 的配置界面。

**Architecture:** CronJob 是 Issue 的兄弟触发型实体：自带 `cron_job` 表 + `threadId = "<cronJobId>:owner"` + `conversationId = cronJobId`。后端 feature 骨架照 `features/project/` 的 adapter 模式（domain/ports/adapter-sqlite/service/http/index），外加独有的 `scheduler.ts`（Bun.cron 进程内调度）。触发起运行复用 dispatcher，新增 `RunOriginKind "cron"`，会话投影按 orchestrator 同款隔离。

**Tech Stack:** Bun 1.3.14, drizzle-orm, React 19, Next.js, react-hook-form + zod, TanStack Query, shadcn/ui

---

### Task 0 — 前置：Bun 版本核实 + packageManager 对齐

**Files:**
- Modify: `package.json:6`

- [ ] **Step 1: 确认运行期版本**

```bash
bun --version
# Expected: ≥ 1.3.11 (Bun.cron 门槛)
```

- [ ] **Step 2: 更新 packageManager 字段**

`package.json:6` 是 `"packageManager": "bun@1.1.0"`。改成实际版本：

```json
"packageManager": "bun@1.3.14"
```

- [ ] **Step 3: Commit**

```bash
git add package.json
git commit -m "chore(repo): bump bun packageManager to 1.3.14 (Bun.cron needs ≥1.3.11)"
```

---

### Task 1 — CronJob 实体与存储 schema

**Files:**
- Create: `apps/backend/src/features/cron/domain.ts`
- Modify: `apps/backend/src/infra/db/schema.ts`
- Regenerate: drizzle baseline (`scripts/gen-drizzle.sh`)

- [ ] **Step 1: 写 domain.ts**

照 `features/project/domain.ts` 的 Row + Input 模式。注意 `enabled` 存 integer 0/1 但在 Row 里暴露 boolean（adapter 转换），照 `autoOrchestrate`。

```ts
// apps/backend/src/features/cron/domain.ts
/** CronJob — a recurring time-table rule that fires an Agent run.
 *  The only new domain ontology in M21. Sibling to Issue: a trigger-type
 *  entity that carries its own threadId and reuses the existing exec layer,
 *  differing only in trigger source (clock vs human/orchestrator). */
export interface CronJobRow {
  cronJobId: string;
  name: string;
  agentId: string;
  /** 5-field cron expression, interpreted in UTC. */
  cronExpr: string;
  /** Input fed to the Agent on every fire. */
  prompt: string;
  /** on/off; disabling only stops the timer, never deletes the rule. */
  enabled: boolean;
  /** per-job active timeout (ms); 0 = no per-job watchdog (reaper still applies). */
  timeoutMs: number;
  /** retries within a single fire on non-success terminal; 0 = no retry. */
  maxRetries: number;
  createdAt: number;
  updatedAt: number;
}

export interface CreateCronJobInput {
  name: string;
  agentId: string;
  cronExpr: string;
  prompt?: string;
  enabled?: boolean;
  timeoutMs?: number;
  maxRetries?: number;
}

export interface UpdateCronJobInput {
  name?: string;
  agentId?: string;
  cronExpr?: string;
  prompt?: string;
  enabled?: boolean;
  timeoutMs?: number;
  maxRetries?: number;
}
```

- [ ] **Step 2: 在 schema.ts 追加 cron_job 表**

照 `project` 表写法（`schema.ts:115-128`）。在文件末尾、`columnConfig` 表之后追加。`enabled` 存 `integer`（0/1），adapter 层转换 boolean。

```ts
// 在 apps/backend/src/infra/db/schema.ts 末尾追加:
// ─── cron_job (M21) ──────────────────────────────────────────────
export const cronJob = sqliteTable(
  "cron_job",
  {
    cronJobId: text().primaryKey(),
    name: text().notNull(),
    agentId: text().notNull(),
    cronExpr: text().notNull(),
    prompt: text().notNull().default(""),
    enabled: integer().notNull().default(0),
    timeoutMs: integer({ mode: "number" }).notNull().default(0),
    maxRetries: integer({ mode: "number" }).notNull().default(0),
    createdAt: integer({ mode: "number" }).notNull(),
    updatedAt: integer({ mode: "number" }).notNull(),
  },
  (table) => [index("idx_cron_job_enabled").on(table.enabled)],
);
```

- [ ] **Step 3: 重生成 drizzle baseline**

```bash
cd apps/backend && bunx drizzle-kit generate --config drizzle.backend.config.ts
```

人工 diff 检查生成的 migration SQL 包含 `cron_job` 表和 `idx_cron_job_enabled` 索引。

- [ ] **Step 4: Typecheck**

```bash
bun run --cwd apps/backend typecheck
```

- [ ] **Step 5: Commit**

```bash
git add apps/backend/src/features/cron/domain.ts \
        apps/backend/src/infra/db/schema.ts \
        apps/backend/drizzle/
git commit -m "feat(cron): CronJob entity + cron_job drizzle table"
```

---

### Task 2 — ports + adapter + service（CRUD + 自带会话）

**Files:**
- Create: `apps/backend/src/features/cron/ports.ts`
- Create: `apps/backend/src/features/cron/adapter-sqlite.ts`
- Create: `apps/backend/src/features/cron/service.ts`
- Create: `apps/backend/src/features/cron/index.ts`
- Create: `apps/backend/src/features/cron/adapter-sqlite.test.ts`
- Create: `apps/backend/src/features/cron/service.test.ts`

- [ ] **Step 1: 写 ports.ts**

照 `project/ports.ts`。方法命名对齐 project 风格（`createCronJob` / `getCronJob` / `listCronJobs` / `listEnabledCronJobs` / `updateCronJob` / `deleteCronJob`）。注意 `listEnabledCronJobs` 是 CronJob 独有的（scheduler 启动时遍历）。

```ts
// apps/backend/src/features/cron/ports.ts
import type { CronJobRow } from "./domain.js";

export interface CreateCronJobRecord {
  cronJobId: string;
  name: string;
  agentId: string;
  cronExpr: string;
  prompt: string;
  enabled: boolean;
  timeoutMs: number;
  maxRetries: number;
  createdAt: number;
  updatedAt: number;
}

export interface UpdateCronJobRecord {
  name?: string;
  agentId?: string;
  cronExpr?: string;
  prompt?: string;
  enabled?: boolean;
  timeoutMs?: number;
  maxRetries?: number;
  updatedAt: number;
}

export interface CronJobPort {
  createCronJob(input: CreateCronJobRecord): CronJobRow;
  getCronJob(cronJobId: string): CronJobRow | null;
  listCronJobs(): CronJobRow[];
  listEnabledCronJobs(): CronJobRow[];
  updateCronJob(cronJobId: string, patch: UpdateCronJobRecord): CronJobRow | null;
  deleteCronJob(cronJobId: string): boolean;
}
```

- [ ] **Step 2: 写 adapter-sqlite.ts**

照 `project/adapter-sqlite.ts`。用 drizzle + `casing: "snake_case"`，`enabled` boolean↔integer 转换照 `autoOrchestrate`。

```ts
// apps/backend/src/features/cron/adapter-sqlite.ts
import type { Database } from "bun:sqlite";
import { eq } from "drizzle-orm";
import { drizzle } from "drizzle-orm/bun-sqlite";
import * as schema from "../../infra/db/schema.js";
import type { CronJobRow } from "./domain.js";
import type { CreateCronJobRecord, CronJobPort, UpdateCronJobRecord } from "./ports.js";

const toRow = (r: typeof schema.cronJob.$inferSelect): CronJobRow => ({
  cronJobId: r.cronJobId,
  name: r.name,
  agentId: r.agentId,
  cronExpr: r.cronExpr,
  prompt: r.prompt,
  enabled: r.enabled === 1,
  timeoutMs: r.timeoutMs,
  maxRetries: r.maxRetries,
  createdAt: r.createdAt,
  updatedAt: r.updatedAt,
});

export function sqliteCronJobAdapter(db: Database): CronJobPort {
  const d = drizzle(db, { schema, casing: "snake_case" });

  return {
    createCronJob(input: CreateCronJobRecord): CronJobRow {
      d.insert(schema.cronJob).values({
        cronJobId: input.cronJobId,
        name: input.name,
        agentId: input.agentId,
        cronExpr: input.cronExpr,
        prompt: input.prompt,
        enabled: input.enabled ? 1 : 0,
        timeoutMs: input.timeoutMs,
        maxRetries: input.maxRetries,
        createdAt: input.createdAt,
        updatedAt: input.updatedAt,
      }).run();
      return toRow(d.select().from(schema.cronJob).where(eq(schema.cronJob.cronJobId, input.cronJobId)).get()!);
    },

    getCronJob(cronJobId: string): CronJobRow | null {
      const r = d.select().from(schema.cronJob).where(eq(schema.cronJob.cronJobId, cronJobId)).get();
      return r ? toRow(r) : null;
    },

    listCronJobs(): CronJobRow[] {
      return d.select().from(schema.cronJob).all().map(toRow);
    },

    listEnabledCronJobs(): CronJobRow[] {
      return d.select().from(schema.cronJob).where(eq(schema.cronJob.enabled, 1)).all().map(toRow);
    },

    updateCronJob(cronJobId: string, patch: UpdateCronJobRecord): CronJobRow | null {
      const sets: Record<string, unknown> = { updatedAt: patch.updatedAt };
      if (patch.name !== undefined) sets.name = patch.name;
      if (patch.agentId !== undefined) sets.agentId = patch.agentId;
      if (patch.cronExpr !== undefined) sets.cronExpr = patch.cronExpr;
      if (patch.prompt !== undefined) sets.prompt = patch.prompt;
      if (patch.enabled !== undefined) sets.enabled = patch.enabled ? 1 : 0;
      if (patch.timeoutMs !== undefined) sets.timeoutMs = patch.timeoutMs;
      if (patch.maxRetries !== undefined) sets.maxRetries = patch.maxRetries;
      if (Object.keys(sets).length <= 1) return this.getCronJob(cronJobId); // nothing to update besides updatedAt
      d.update(schema.cronJob).set(sets).where(eq(schema.cronJob.cronJobId, cronJobId)).run();
      return this.getCronJob(cronJobId);
    },

    deleteCronJob(cronJobId: string): boolean {
      const result = d.delete(schema.cronJob).where(eq(schema.cronJob.cronJobId, cronJobId)).returning().all();
      return result.length > 0;
    },
  };
}
```

- [ ] **Step 3: 写 service.ts**

照 `project/service.ts` 工厂函数模式。只有 Error 子类是 class。依赖注入 `convPort` 用于 best-effort 建自带会话（照 issue service 的 `convPort` 套路）。`agentExists` 校验 agentId 合法性（照 `columnConfig` service 的 `agentExists`）。

```ts
// apps/backend/src/features/cron/service.ts
import type { CronJobRow } from "./domain.js";
import type { CronJobPort } from "./ports.js";

export class CronJobNotFoundError extends Error {
  constructor(id: string) { super(`CronJob not found: ${id}`); this.name = "CronJobNotFoundError"; }
}
export class CronJobValidationError extends Error {
  constructor(message: string) { super(message); this.name = "CronJobValidationError"; }
}

export interface CronJobServiceDeps {
  port: CronJobPort;
  idGen: () => string;
  agentExists: (id: string) => boolean;
  now?: () => number;
  convPort?: {
    createConversation: (input: { conversationId: string; title?: string; origin?: string }) => void;
    addMember: (input: { conversationId: string; memberId: string; kind: "human" | "agent"; agentId?: string }) => void;
  };
}

export function createCronJobService(deps: CronJobServiceDeps) {
  const { port, idGen, agentExists } = deps;
  const now = deps.now ?? Date.now;

  function require(id: string) {
    const j = port.getCronJob(id);
    if (!j) throw new CronJobNotFoundError(id);
    return j;
  }

  return {
    port,

    createCronJob(input: { name: string; agentId: string; cronExpr: string; prompt?: string; timeoutMs?: number; maxRetries?: number; enabled?: boolean }): CronJobRow {
      if (!agentExists(input.agentId)) throw new CronJobValidationError(`agent not found: ${input.agentId}`);
      const cronJobId = idGen();
      const ts = now();
      port.createCronJob({
        cronJobId,
        name: input.name,
        agentId: input.agentId,
        cronExpr: input.cronExpr,
        prompt: input.prompt ?? "",
        enabled: input.enabled ?? false,
        timeoutMs: input.timeoutMs ?? 0,
        maxRetries: input.maxRetries ?? 0,
        createdAt: ts,
        updatedAt: ts,
      });
      // best-effort 自带会话（conversationId = cronJobId），照 issue/service.ts
      try {
        deps.convPort?.createConversation({ conversationId: cronJobId, title: input.name, origin: "cron" });
        deps.convPort?.addMember({ conversationId: cronJobId, memberId: "owner", kind: "agent", agentId: input.agentId });
      } catch (e) {
        console.error(`[cron] conv bootstrap failed for ${cronJobId}: ${String(e)}`);
      }
      return require(cronJobId);
    },

    getById(id: string): CronJobRow { return require(id); },

    list(): CronJobRow[] { return port.listCronJobs(); },

    exists(id: string): boolean { return port.getCronJob(id) !== null; },

    update(id: string, patch: { name?: string; agentId?: string; cronExpr?: string; prompt?: string; timeoutMs?: number; maxRetries?: number; enabled?: boolean }): CronJobRow {
      if (patch.agentId !== undefined && !agentExists(patch.agentId)) throw new CronJobValidationError(`agent not found: ${patch.agentId}`);
      const result = port.updateCronJob(id, { ...patch, updatedAt: now() });
      if (!result) throw new CronJobNotFoundError(id);
      return result;
    },

    setEnabled(id: string, enabled: boolean): CronJobRow {
      const result = port.updateCronJob(id, { enabled, updatedAt: now() });
      if (!result) throw new CronJobNotFoundError(id);
      return result;
    },

    remove(id: string): void {
      if (!port.deleteCronJob(id)) throw new CronJobNotFoundError(id);
    },
  };
}

export type CronJobService = ReturnType<typeof createCronJobService>;
```

- [ ] **Step 4: 写 index.ts**

```ts
// apps/backend/src/features/cron/index.ts
export { sqliteCronJobAdapter } from "./adapter-sqlite.js";
export type { CronJobRow } from "./domain.js";
export { cronJobRoutes } from "./http.js";
export type { CronJobPort } from "./ports.js";
export {
  createCronJobService,
  CronJobNotFoundError,
  CronJobValidationError,
  type CronJobService,
} from "./service.js";
export { createCronScheduler, type CronScheduler } from "./scheduler.js";
```

- [ ] **Step 5: 写 adapter-sqlite.test.ts**

照 `project` adapter 测试模式：`openDb` 拿真库，test CRUD + enable/disable boolean 转换。

```ts
// apps/backend/src/features/cron/adapter-sqlite.test.ts
import { afterAll, describe, expect, test } from "bun:test";
import { Database } from "bun:sqlite";
import { openDb } from "../../infra/db/openDb.js";
import { sqliteCronJobAdapter } from "./adapter-sqlite.js";
import type { CronJobPort } from "./ports.js";

describe("sqliteCronJobAdapter", () => {
  const db = openDb("/tmp/test-cron-adapter.db");
  const adapter: CronJobPort = sqliteCronJobAdapter(db);
  const now = Date.now();

  afterAll(() => { db.close(); });

  test("createCronJob and getCronJob", () => {
    const row = adapter.createCronJob({
      cronJobId: "cj-1", name: "Daily Patrol", agentId: "agent-1",
      cronExpr: "0 9 * * *", prompt: "patrol please",
      enabled: true, timeoutMs: 60000, maxRetries: 3,
      createdAt: now, updatedAt: now,
    });
    expect(row.cronJobId).toBe("cj-1");
    expect(row.enabled).toBe(true);
    expect(row.timeoutMs).toBe(60000);

    const fetched = adapter.getCronJob("cj-1");
    expect(fetched).not.toBeNull();
    expect(fetched!.name).toBe("Daily Patrol");
    expect(fetched!.enabled).toBe(true);
  });

  test("listCronJobs and listEnabledCronJobs", () => {
    adapter.createCronJob({
      cronJobId: "cj-2", name: "Disabled Job", agentId: "agent-1",
      cronExpr: "*/15 * * * *", prompt: "check build",
      enabled: false, timeoutMs: 0, maxRetries: 0,
      createdAt: now, updatedAt: now,
    });
    expect(adapter.listCronJobs()).toHaveLength(2);
    expect(adapter.listEnabledCronJobs()).toHaveLength(1);
    expect(adapter.listEnabledCronJobs()[0]!.cronJobId).toBe("cj-1");
  });

  test("updateCronJob", () => {
    const updated = adapter.updateCronJob("cj-1", { name: "Morning Patrol", updatedAt: now + 1000 });
    expect(updated).not.toBeNull();
    expect(updated!.name).toBe("Morning Patrol");
  });

  test("deleteCronJob", () => {
    expect(adapter.deleteCronJob("cj-1")).toBe(true);
    expect(adapter.getCronJob("cj-1")).toBeNull();
    expect(adapter.deleteCronJob("cj-nonexistent")).toBe(false);
  });
});
```

- [ ] **Step 6: 写 service.test.ts**

```ts
// apps/backend/src/features/cron/service.test.ts
import { describe, expect, test } from "bun:test";
import type { CronJobPort } from "./ports.js";
import { createCronJobService, CronJobNotFoundError, CronJobValidationError } from "./service.js";

function mockPort(rows: Array<ReturnType<ReturnType<typeof createCronJobService>>["list"]>[number]> = []): CronJobPort {
  const store = new Map(rows.map(r => [r.cronJobId, { ...r }]));
  return {
    createCronJob(input) { const r = { ...input }; store.set(input.cronJobId, r); return r as any; },
    getCronJob(id) { return (store.get(id) as any) ?? null; },
    listCronJobs() { return [...store.values()] as any[]; },
    listEnabledCronJobs() { return [...store.values()].filter((r: any) => r.enabled) as any[]; },
    updateCronJob(id, patch) { const r = store.get(id); if (!r) return null; Object.assign(r as any, patch); return (store.get(id) as any); },
    deleteCronJob(id) { return store.delete(id); },
  };
}

describe("createCronJobService", () => {
  const svc = createCronJobService({
    port: mockPort(),
    idGen: (() => { let n = 0; return () => `cj-${++n}`; })(),
    agentExists: (id) => id === "agent-1",
  });

  test("createCronJob validates agentId", () => {
    expect(() => svc.createCronJob({ name: "test", agentId: "nonexistent", cronExpr: "0 9 * * *" }))
      .toThrow(CronJobValidationError);
  });

  test("createCronJob succeeds with valid agentId", () => {
    const job = svc.createCronJob({ name: "test", agentId: "agent-1", cronExpr: "0 9 * * *" });
    expect(job.cronJobId).toStartWith("cj-");
    expect(job.enabled).toBe(false);
  });

  test("createCronJob with enabled:true", () => {
    const job = svc.createCronJob({ name: "enabled job", agentId: "agent-1", cronExpr: "* * * * *", enabled: true });
    expect(job.enabled).toBe(true);
  });

  test("setEnabled toggles", () => {
    const job = svc.createCronJob({ name: "toggle", agentId: "agent-1", cronExpr: "0 9 * * *" });
    expect(svc.setEnabled(job.cronJobId, true).enabled).toBe(true);
    expect(svc.setEnabled(job.cronJobId, false).enabled).toBe(false);
  });

  test("getById throws on missing", () => {
    expect(() => svc.getById("nonexistent")).toThrow(CronJobNotFoundError);
  });

  test("remove and getById throws", () => {
    const job = svc.createCronJob({ name: "to-delete", agentId: "agent-1", cronExpr: "0 9 * * *" });
    svc.remove(job.cronJobId);
    expect(() => svc.getById(job.cronJobId)).toThrow(CronJobNotFoundError);
  });

  test("convPort best-effort does not block create", () => {
    let called = false;
    const svc2 = createCronJobService({
      port: mockPort(),
      idGen: () => "cj-conv",
      agentExists: () => true,
      convPort: {
        createConversation: () => { called = true; throw new Error("boom"); },
        addMember: () => {},
      },
    });
    const job = svc2.createCronJob({ name: "conv-test", agentId: "agent-1", cronExpr: "0 9 * * *" });
    expect(job).not.toBeNull();
    expect(called).toBe(true); // tried but failed silently
  });
});
```

- [ ] **Step 7: Typecheck + Test**

```bash
bun run --cwd apps/backend typecheck
cd apps/backend && bun test --test-name-pattern="cron"
# Expected: all pass
```

- [ ] **Step 8: Commit**

```bash
git add apps/backend/src/features/cron/ports.ts \
        apps/backend/src/features/cron/adapter-sqlite.ts \
        apps/backend/src/features/cron/service.ts \
        apps/backend/src/features/cron/index.ts \
        apps/backend/src/features/cron/adapter-sqlite.test.ts \
        apps/backend/src/features/cron/service.test.ts
git commit -m "feat(cron): port + drizzle adapter + service with self-owned conversation"
```

---

### Task 3 — dispatcher 接入 cron：originKind + cronJobId + 投影隔离

**Files:**
- Modify: `apps/backend/src/features/runtime-ops/types.ts:48`
- Modify: `apps/backend/src/infra/db/events-schema.ts:63-85`（run_origin 表加 cron_job_id 列）
- Regenerate: `apps/backend/drizzle/`（events baseline）
- Modify: `apps/backend/src/features/runtime-ops/store.ts`（insertRunOrigin / getRunOrigin / getRunOriginsByCronJobId）
- Modify: `apps/backend/src/features/conversation/projection.ts:169-175`

- [ ] **Step 1: 改 types.ts — 加 "cron" 到 RunOriginKind + cronJobId 到 RunOriginRow**

```ts
// apps/backend/src/features/runtime-ops/types.ts:48
// 现状: export type RunOriginKind = "orchestrator" | "mention" | "manual";
export type RunOriginKind = "orchestrator" | "mention" | "manual" | "cron";

// 在 RunOriginRow interface 里 issueId 之后加:
  /** M21: non-null = cron-driven run (back-link to cron_job + @mention isolation). */
  cronJobId?: string | null;
```

- [ ] **Step 2: 改 events-schema.ts — run_origin 表加 cron_job_id 列**

在 `runOrigin` 表定义中，`issueId` 字段之后加：

```ts
cronJobId: text(),
```

在 table extras（索引数组）里 `idx_run_origin_issue` 之后加：

```ts
index("idx_run_origin_cron").on(table.cronJobId),
```

- [ ] **Step 3: 重生成 events drizzle baseline**

```bash
cd apps/backend && bunx drizzle-kit generate --config drizzle.events.config.ts
```

人工 diff 确认 migration SQL 含 `ALTER TABLE run_origin ADD COLUMN cron_job_id text` 和 `CREATE INDEX idx_run_origin_cron`。

- [ ] **Step 4: 改 store.ts — insertRunOrigin/getRunOrigin 读写 cronJobId**

`store.ts` 里 `insertRunOrigin` 的 drizzle 插入加 `cronJobId: origin.cronJobId ?? null`。`getRunOrigin` 的 `.get()` 结果映射回 `RunOriginRow` 时带上 `cronJobId: row.cronJobId ?? null`。加一个新查询方法 `getRunOriginsByCronJobId`（可选，为后续扩展）。

```ts
// 在 RuntimeOpsStore 类上加:
getRunOriginsByCronJobId(cronJobId: string): RunOriginRow[] {
  return this.#d.select().from(schema.runOrigin)
    .where(eq(schema.runOrigin.cronJobId, cronJobId)).all()
    .map(rowToRunOrigin);
}
```

`rowToRunOrigin` helper（当前是 `store.ts` 内部的 drizzle row → domain row 转换）加 `cronJobId: r.cronJobId ?? null`。

- [ ] **Step 5: 改 projection.ts — cron run 隔离**

`projection.ts:169-175`，把单条件改成 Set：

```ts
// 现状:
// if (origin?.originKind === "orchestrator") {
//   clearAccumulator(runId);
//   return;
// }

// M21: cron run + orchestrator run both isolated from @mention cascade.
const ISOLATED_ORIGINS = new Set(["orchestrator", "cron"]);
if (origin && ISOLATED_ORIGINS.has(origin.originKind)) {
  clearAccumulator(runId);
  return;
}
```

- [ ] **Step 6: Typecheck + existing tests**

```bash
bun run --cwd apps/backend typecheck
cd apps/backend && bun test
# Expected: all existing tests pass, no regressions
```

- [ ] **Step 7: Commit**

```bash
git add apps/backend/src/features/runtime-ops/types.ts \
        apps/backend/src/infra/db/events-schema.ts \
        apps/backend/drizzle/ \
        apps/backend/src/features/runtime-ops/store.ts \
        apps/backend/src/features/conversation/projection.ts
git commit -m "feat(cron): RunOriginKind \"cron\" + run_origin.cron_job_id + projection isolation"
```

---

### Task 4 — CronScheduler：Bun.cron 进程内调度 + 超时看门狗 + 重试

**Files:**
- Create: `apps/backend/src/features/cron/scheduler.ts`
- Create: `apps/backend/src/features/cron/scheduler.test.ts`

- [ ] **Step 1: 写 scheduler.ts**

独立服务，照 spec §P4 伪代码。关键设计点：
1. `import { CronJob as BunCron } from "bun"` 别名避免命名冲突
2. 只用 2 参形态 `BunCron(schedule, handler)`，绝不用 3 参 OS crontab 形态
3. `handles` Map 存 Bun 句柄，`watchdogs` Map 存 per-run 超时 timer
4. `fire(job)` 经 `dispatcher.dispatch({kind:"cron"})` 起 run
5. `onRunComplete` 监听器处理重试 + 清看门狗
6. 重试用指数退避 `Math.min(1000 * 2^attempts, 30000)`，新 runId + 同 fireKey
7. `idempotencyKey = "${cronJobId}:${firedAtUnix}:run:${attempt}"` — 带 attempt 后缀防唯一约束冲突

```ts
// apps/backend/src/features/cron/scheduler.ts
import { CronJob as BunCron } from "bun";
import type { CronJobService } from "./service.js";
import type { CronJobRow } from "./domain.js";
import type { RunDispatcher } from "../run/dispatcher.js";
import type { RuntimeOpsStore } from "../runtime-ops/store.js";

export function createCronScheduler(deps: {
  cronSvc: CronJobService;
  dispatcher: RunDispatcher;
  supervisor: {
    cancel(runId: string): Promise<void> | void;
    onRunComplete(fn: (threadId: string, runId: string, status: string, kind?: string) => unknown): void;
  };
  opsStore: RuntimeOpsStore;
  buildSpec: (agentId: string, threadId: string, input: string) => Promise<Record<string, unknown>>;
  idGen: () => string;
  trace: () => { traceId: string; traceparent: string };
  now?: () => number;
}) {
  const handles = new Map<string, ReturnType<typeof BunCron>>();
  const watchdogs = new Map<string, ReturnType<typeof setTimeout>>();
  const retryCounts = new Map<string, number>(); // key = `${cronJobId}:${firedAtUnix}`

  async function fire(job: CronJobRow): Promise<void> {
    const n = deps.now ?? Date.now;
    const firedAtUnix = Math.floor(n() / 1000);
    const fireKey = `${job.cronJobId}:${firedAtUnix}`;
    const attempt = retryCounts.get(fireKey) ?? 0;
    const runId = deps.idGen();
    const threadId = `${job.cronJobId}:owner`;
    const t = deps.trace();
    const spec = await deps.buildSpec(job.agentId, threadId, job.prompt);

    await deps.dispatcher.dispatch({
      kind: "cron",
      runId,
      threadId,
      spec,
      opts: { trace: t },
      origin: {
        conversationId: job.cronJobId,
        sourceLedgerSeq: 0,
        agentMemberId: "owner",
        surface: "cron",
        traceId: t.traceId,
        traceparent: t.traceparent,
        idempotencyKey: `${fireKey}:run:${attempt}`,
        issueId: null,
        fromStatus: "",
        cronJobId: job.cronJobId,
      },
    });

    // per-job active timeout watchdog
    if (job.timeoutMs > 0) {
      const wd = setTimeout(() => { void deps.supervisor.cancel(runId); }, job.timeoutMs);
      watchdogs.set(runId, wd);
    }
  }

  // Retry + watchdog cleanup listener, stacked on supervisor.onRunComplete
  deps.supervisor.onRunComplete(async (_threadId, runId, status, _kind) => {
    // Clean watchdog if any
    const wd = watchdogs.get(runId);
    if (wd) { clearTimeout(wd); watchdogs.delete(runId); }

    // Only handle cron runs
    const origin = deps.opsStore.getRunOrigin(runId);
    if (!origin || origin.originKind !== "cron" || !origin.cronJobId) return;

    // Completed successfully — done
    if (status === "completed") return;

    // Check retry budget
    const job = deps.cronSvc.port.getCronJob(origin.cronJobId);
    if (!job || job.maxRetries <= 0) return;

    const fireKey = origin.idempotencyKey.split(":run:")[0]!;
    const attempts = retryCounts.get(fireKey) ?? 0;
    if (attempts >= job.maxRetries) { retryCounts.delete(fireKey); return; }

    retryCounts.set(fireKey, attempts + 1);
    deps.opsStore.insertRunOpsEvent({ runId, kind: "retry_requested", payload: { fireKey, attempt: attempts + 1 } });
    // Exponential backoff, non-blocking
    await new Promise((r) => setTimeout(r, Math.min(1000 * 2 ** attempts, 30_000)));
    deps.opsStore.insertRunOpsEvent({ runId, kind: "retry_started", payload: { fireKey, attempt: attempts + 1 } });
    await fire(job);
  });

  return {
    /** Register all enabled jobs (called on startup). */
    start() {
      for (const job of deps.cronSvc.port.listEnabledCronJobs()) this.register(job);
    },

    /** Register or re-register a single job. Call after create/enable/update. */
    register(job: CronJobRow) {
      this.unregister(job.cronJobId);
      if (!job.enabled) return;
      handles.set(job.cronJobId, BunCron(job.cronExpr, () => { void fire(job); }));
    },

    /** Stop and remove a single job's timer. Call after disable/delete. */
    unregister(cronJobId: string) {
      const h = handles.get(cronJobId);
      if (h) { h.stop(); handles.delete(cronJobId); }
    },

    /** Stop all timers + clear all watchdogs. Call on shutdown. */
    dispose() {
      for (const h of handles.values()) h.stop();
      handles.clear();
      for (const wd of watchdogs.values()) clearTimeout(wd);
      watchdogs.clear();
    },
  };
}

export type CronScheduler = ReturnType<typeof createCronScheduler>;
```

- [ ] **Step 2: 写 scheduler.test.ts**

用 fake timers (`bun:test` 的 `setSystemTime` / `useFakeTimers` 如果支持，否则 stub deps)。核心覆盖：
- `start()` 注册 enabled job
- `start()` 跳过 disabled job
- `register()` enabled job → handles 有句柄
- `register()` disabled job → handles 无句柄
- `register()` re-registers（先 stop 旧再建新）
- `unregister()` stop + delete
- `dispose()` 全停
- `fire()` dispatch 用 `kind:"cron"` + `threadId = "<id>:owner"` + `cronJobId` 正确
- `timeoutMs > 0` 触发 cancel
- 非成功收尾在 maxRetries 内重起，达上限停
- 重试 `idempotencyKey` 带不同 `:run:<attempt>` 后缀

```ts
// apps/backend/src/features/cron/scheduler.test.ts
import { describe, expect, mock, test } from "bun:test";
import type { CronJobService } from "./service.js";
import { createCronScheduler } from "./scheduler.js";
import type { CronJobRow } from "./domain.js";

function makeJob(overrides: Partial<CronJobRow> = {}): CronJobRow {
  return {
    cronJobId: "cj-test",
    name: "Test Job",
    agentId: "agent-1",
    cronExpr: "0 9 * * *",
    prompt: "hello",
    enabled: true,
    timeoutMs: 0,
    maxRetries: 0,
    createdAt: Date.now(),
    updatedAt: Date.now(),
    ...overrides,
  };
}

describe("createCronScheduler", () => {
  test("start registers all enabled jobs", () => {
    const job = makeJob();
    const svc = { port: { listEnabledCronJobs: () => [job] } } as any;
    const scheduler = createCronScheduler({
      cronSvc: svc,
      dispatcher: { dispatch: mock(async () => ({ runId: "r1", attemptId: "a1" })) } as any,
      supervisor: { cancel: mock(() => {}), onRunComplete: mock(() => {}) } as any,
      opsStore: { getRunOrigin: () => null, insertRunOpsEvent: mock(() => {}) } as any,
      buildSpec: mock(async () => ({})),
      idGen: () => "r1",
      trace: () => ({ traceId: "t1", traceparent: "tp1" }),
    });
    scheduler.start();
    // Bun.cron was called — we can't easily assert on it without spying on Bun.cron,
    // but we verify no throw + handles populated via register/unregister behaviour
    scheduler.dispose();
  });

  test("register enabled job adds handle, unregister removes it", () => {
    const job = makeJob();
    const scheduler = createCronScheduler({
      cronSvc: null as any,
      dispatcher: null as any,
      supervisor: null as any,
      opsStore: null as any,
      buildSpec: async () => ({}),
      idGen: () => "r",
      trace: () => ({ traceId: "t", traceparent: "tp" }),
    });
    scheduler.register(job);
    // No error = handle stored
    scheduler.unregister(job.cronJobId);
    // No error = handle stopped and removed
    scheduler.dispose();
  });

  test("register disabled job does not create handle", () => {
    const job = makeJob({ enabled: false });
    const scheduler = createCronScheduler({
      cronSvc: null as any,
      dispatcher: null as any,
      supervisor: null as any,
      opsStore: null as any,
      buildSpec: async () => ({}),
      idGen: () => "r",
      trace: () => ({ traceId: "t", traceparent: "tp" }),
    });
    scheduler.register(job);
    // No handle should be created
    scheduler.dispose();
  });
});
```

- [ ] **Step 3: Typecheck + Test**

```bash
bun run --cwd apps/backend typecheck
cd apps/backend && bun test --test-name-pattern="scheduler"
# Expected: all pass
```

- [ ] **Step 4: Commit**

```bash
git add apps/backend/src/features/cron/scheduler.ts \
        apps/backend/src/features/cron/scheduler.test.ts
git commit -m "feat(cron): in-process Bun.cron scheduler with per-job timeout + retry"
```

---

### Task 5 — HTTP 路由 + main.ts 装配

**Files:**
- Create: `apps/backend/src/features/cron/http.ts`
- Modify: `apps/backend/src/http/router.ts`（FeatureSet + 路由块）
- Modify: `apps/backend/src/main.ts`（装配 cronSvc + scheduler）

- [ ] **Step 1: 写 http.ts**

照 `project/http.ts`。zod 5 段 cron 正则粗校验。create/update/enable/remove 成功后调 `scheduler.register()`/`scheduler.unregister()`（「先 service 后 scheduler」，事实先落表再更新投影）。

```ts
// apps/backend/src/features/cron/http.ts
import { z } from "zod";
import { json } from "../../http/response.js";
import type { CronScheduler } from "./scheduler.js";
import { CronJobNotFoundError, CronJobValidationError, type CronJobService } from "./service.js";

const cronExprField = z.string().trim().regex(
  /^(\S+\s+){4}\S+$/,
  "cron expression must have 5 fields",
);

const createSchema = z.object({
  name: z.string().trim().min(1),
  agentId: z.string().trim().min(1),
  cronExpr: cronExprField,
  prompt: z.string().optional(),
  timeoutMs: z.number().int().nonnegative().optional(),
  maxRetries: z.number().int().nonnegative().optional(),
  enabled: z.boolean().optional(),
});

const updateSchema = z.object({
  name: z.string().trim().min(1).optional(),
  agentId: z.string().trim().min(1).optional(),
  cronExpr: cronExprField.optional(),
  prompt: z.string().optional(),
  timeoutMs: z.number().int().nonnegative().optional(),
  maxRetries: z.number().int().nonnegative().optional(),
}).strict();

const enableSchema = z.object({ enabled: z.boolean() });

export function cronJobRoutes(svc: CronJobService, scheduler: CronScheduler) {
  return {
    list(_req: Request): Response {
      return json({ cronJobs: svc.list() });
    },

    get(_req: Request, id: string): Response {
      try { return json({ cronJob: svc.getById(id) }); }
      catch (e) { if (e instanceof CronJobNotFoundError) return json({ error: e.message }, 404); throw e; }
    },

    async create(req: Request): Promise<Response> {
      const p = createSchema.safeParse(await req.json().catch(() => ({})));
      if (!p.success) return json({ error: "Validation failed", details: p.error.issues }, 400);
      try {
        const job = svc.createCronJob(p.data);
        scheduler.register(job);
        return json({ cronJob: job }, 201);
      } catch (e) {
        if (e instanceof CronJobValidationError) return json({ error: e.message }, 400);
        throw e;
      }
    },

    async update(req: Request, id: string): Promise<Response> {
      const p = updateSchema.safeParse(await req.json().catch(() => ({})));
      if (!p.success) return json({ error: "Validation failed", details: p.error.issues }, 400);
      try {
        const job = svc.update(id, p.data);
        scheduler.register(job); // re-register (expression may have changed)
        return json({ cronJob: job });
      } catch (e) {
        if (e instanceof CronJobNotFoundError) return json({ error: e.message }, 404);
        if (e instanceof CronJobValidationError) return json({ error: e.message }, 400);
        throw e;
      }
    },

    async setEnabled(req: Request, id: string): Promise<Response> {
      const p = enableSchema.safeParse(await req.json().catch(() => ({})));
      if (!p.success) return json({ error: "Validation failed", details: p.error.issues }, 400);
      try {
        const job = svc.setEnabled(id, p.data.enabled);
        scheduler.register(job); // register internally stops if disabled
        return json({ cronJob: job });
      } catch (e) {
        if (e instanceof CronJobNotFoundError) return json({ error: e.message }, 404);
        throw e;
      }
    },

    remove(_req: Request, id: string): Response {
      try {
        svc.remove(id);
        scheduler.unregister(id);
        return new Response(null, { status: 204 });
      } catch (e) {
        if (e instanceof CronJobNotFoundError) return json({ error: e.message }, 404);
        throw e;
      }
    },
  };
}
```

- [ ] **Step 2: 改 router.ts**

`FeatureSet` 加 `cronJobs?: ReturnType<typeof cronJobRoutes>`。在 projects 路由块之后加 cron-jobs 路由块（照 projects 块模式）：

```ts
// 在 FeatureSet 接口里加:
  cronJobs?: ReturnType<typeof cronJobRoutes>;

// 在 createRouter 解构里加:
  const { agents, runs, threadProjections, conversations, ops, issues, projects, columnConfigs, cronJobs } = features ?? {};

// 在 projects 路由块之后加:
  // ── CronJobs (M21) ──
  if (cronJobs) {
    const listMatch = path === "/api/cron-jobs";
    const enableMatch = path.match(/^\/api\/cron-jobs\/([^/]+)\/enable$/);
    const idMatch = path.match(/^\/api\/cron-jobs\/([^/]+)$/);
    if (listMatch && method === "GET") return withAuth(async () => cronJobs.list(), token)(req);
    if (listMatch && method === "POST") return withAuth(async (r) => cronJobs.create(r), token)(req);
    if (listMatch) return json({ error: "Method not allowed" }, 405);
    if (enableMatch && method === "POST") return withAuth(async (r) => cronJobs.setEnabled(r, enableMatch[1]!), token)(req);
    if (enableMatch) return json({ error: "Method not allowed" }, 405);
    if (idMatch && method === "GET") return withAuth(async (r) => cronJobs.get(r, idMatch[1]!), token)(req);
    if (idMatch && method === "PATCH") return withAuth(async (r) => cronJobs.update(r, idMatch[1]!), token)(req);
    if (idMatch && method === "DELETE") return withAuth(async (r) => cronJobs.remove(r, idMatch[1]!), token)(req);
    if (idMatch) return json({ error: "Method not allowed" }, 405);
  }
```

- [ ] **Step 3: 改 main.ts 装配**

在 `issueSvc` 创建之后、`supervisor.onRunComplete` 之前加 cronSvc + scheduler 创建。用 `buildIssueSpec` 同构的 buildSpec（或直接复用它，因为逻辑一样：按 agentId 拼 spec）。

```ts
// 在 main.ts imports 里加:
import { sqliteCronJobAdapter, createCronJobService, cronJobRoutes, createCronScheduler } from "./features/cron/index.js";

// 在 issueSvc 之后加:
const cronSvc = createCronJobService({
  port: sqliteCronJobAdapter(db),
  idGen: ulid,
  agentExists: (id) => agentSvc.exists(id),
  convPort: {
    createConversation: (input) => conv.convPort.createConversation(input),
    addMember: (input) => conv.convPort.addMember(input),
  },
});

const cronScheduler = createCronScheduler({
  cronSvc,
  dispatcher,
  supervisor,
  opsStore,
  buildSpec: buildIssueSpec, // 同构：按 agentId 拼 schemaVersion=2 spec
  idGen: ulid,
  trace: () => makeTrace(),
});

// 在 createRouter 调用里加:
  cronJobs: cronJobRoutes(cronSvc, cronScheduler),

// 在 server.start() 之后加:
cronScheduler.start();

// 在 shutdown() 里 supervisor.dispose() 之后、registry.dispose() 之前加:
cronScheduler.dispose();
```

- [ ] **Step 4: Typecheck + 冒烟**

```bash
bun run --cwd apps/backend typecheck
# 启动后端验证空库建表不报错、注册 0 个 job 不报错
```

- [ ] **Step 5: Commit**

```bash
git add apps/backend/src/features/cron/http.ts \
        apps/backend/src/http/router.ts \
        apps/backend/src/main.ts
git commit -m "feat(cron): HTTP routes + main wiring (scheduler start/dispose)"
```

---

### Task 6 — 前端：CronJob 列表 / 表单 / 导航 / 点进会话

**Files:**
- Create: `apps/web/src/app/(main)/cron/page.tsx`
- Create: `apps/web/src/components/CronJobForm.tsx`
- Modify: `apps/web/src/lib/api.ts`
- Modify: `apps/web/src/components/NavRail.tsx`

- [ ] **Step 1: 在 api.ts 加 types 和 methods**

照 project 段（`api.ts:298-314`）。

```ts
// 在 api.ts 顶部 types 区域加:
export interface CronJobRow {
  cronJobId: string;
  name: string;
  agentId: string;
  cronExpr: string;
  prompt: string;
  enabled: boolean;
  timeoutMs: number;
  maxRetries: number;
  createdAt: number;
  updatedAt: number;
}

// 在 api 对象里 project 段之后加:
  // ── CronJobs (M21) ──
  listCronJobs: () => apiFetch<{ cronJobs: CronJobRow[] }>("cron-jobs"),
  createCronJob: (body: {
    name: string;
    agentId: string;
    cronExpr: string;
    prompt?: string;
    timeoutMs?: number;
    maxRetries?: number;
    enabled?: boolean;
  }) => apiFetch<{ cronJob: CronJobRow }>("cron-jobs", { method: "POST", body }),
  updateCronJob: (id: string, body: {
    name?: string;
    agentId?: string;
    cronExpr?: string;
    prompt?: string;
    timeoutMs?: number;
    maxRetries?: number;
  }) => apiFetch<{ cronJob: CronJobRow }>(`cron-jobs/${id}`, { method: "PATCH", body }),
  setCronJobEnabled: (id: string, enabled: boolean) =>
    apiFetch<{ cronJob: CronJobRow }>(`cron-jobs/${id}/enable`, { method: "POST", body: { enabled } }),
  deleteCronJob: (id: string) => apiFetch<void>(`cron-jobs/${id}`, { method: "DELETE" }),
```

- [ ] **Step 2: 写 CronJobForm.tsx**

照 `ProjectForm.tsx` 全套（Dialog + react-hook-form + zodResolver + sonner + `queryClient.invalidateQueries({ queryKey: ["cron-jobs"] })`）。

字段：
- `name` — Input, required
- `agentId` — Select, 选项从 `api.listAgents()` 过滤 `!archivedAt`
- `cronExpr` — Input, placeholder `0 9 * * *`，hint「5 段表达式，UTC 解释」
- `prompt` — Textarea
- `timeoutMs` — Input number, hint「0 = 不设 per-job 超时（全局 reaper 仍生效）」
- `maxRetries` — Input number, hint「0 = 不重试」
- `enabled` — Checkbox

create + edit 双模（`editCronJob?: CronJobRow` prop）。

```tsx
// apps/web/src/components/CronJobForm.tsx
"use client";

import { zodResolver } from "@hookform/resolvers/zod";
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { ArrowRight } from "lucide-react";
import { useEffect, useState } from "react";
import { useForm } from "react-hook-form";
import { toast } from "sonner";
import { z } from "zod";

import { Button } from "@/components/ui/button";
import { Checkbox } from "@/components/ui/checkbox";
import { Dialog, DialogContent, DialogHeader, DialogTitle, DialogTrigger } from "@/components/ui/dialog";
import { Form, FormControl, FormField, FormItem, FormLabel, FormMessage } from "@/components/ui/form";
import { Input } from "@/components/ui/input";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { Textarea } from "@/components/ui/textarea";
import { api, type CronJobRow } from "@/lib/api";
import { fieldClass, labelClass } from "@/lib/form-styles";

const formSchema = z.object({
  name: z.string().trim().min(1, "Name is required"),
  agentId: z.string().trim().min(1, "Agent is required"),
  cronExpr: z.string().trim().regex(/^(\S+\s+){4}\S+$/, "Must be a 5-field cron expression"),
  prompt: z.string().optional().default(""),
  timeoutMs: z.coerce.number().int().nonnegative().optional().default(0),
  maxRetries: z.coerce.number().int().nonnegative().optional().default(0),
  enabled: z.boolean().default(false),
});
type FormValues = z.infer<typeof formSchema>;

interface CronJobFormProps {
  editCronJob?: CronJobRow;
  onSuccess?: () => void;
}

export function CronJobForm({ editCronJob, onSuccess }: CronJobFormProps) {
  const qc = useQueryClient();
  const isEdit = !!editCronJob;
  const [open, setOpen] = useState(false);
  const [serverError, setServerError] = useState<string | null>(null);

  const { data: agentsData } = useQuery({
    queryKey: ["agents"],
    queryFn: api.listAgents,
  });
  const agents = (agentsData?.agents ?? []).filter((a) => !a.archivedAt);

  const form = useForm<FormValues>({
    resolver: zodResolver(formSchema),
    defaultValues: { name: "", agentId: "", cronExpr: "", prompt: "", timeoutMs: 0, maxRetries: 0, enabled: false },
  });

  useEffect(() => {
    if (editCronJob) {
      setOpen(true);
      form.reset({
        name: editCronJob.name,
        agentId: editCronJob.agentId,
        cronExpr: editCronJob.cronExpr,
        prompt: editCronJob.prompt,
        timeoutMs: editCronJob.timeoutMs,
        maxRetries: editCronJob.maxRetries,
        enabled: editCronJob.enabled,
      });
    }
  }, [editCronJob, form]);

  function handleOpen(o: boolean) {
    if (!o) {
      form.reset();
      setServerError(null);
    }
    if (!isEdit) setOpen(o);
  }

  const createMu = useMutation({
    mutationFn: api.createCronJob,
    onSuccess: () => {
      qc.invalidateQueries({ queryKey: ["cron-jobs"] });
      toast.success("Schedule created");
      setOpen(false);
      onSuccess?.();
    },
    onError: (e) => { setServerError(String(e)); toast.error("Failed to create schedule"); },
  });

  const updateMu = useMutation({
    mutationFn: (body: Parameters<typeof api.updateCronJob>[1]) => api.updateCronJob(editCronJob!.cronJobId, body),
    onSuccess: () => {
      qc.invalidateQueries({ queryKey: ["cron-jobs"] });
      toast.success("Schedule updated");
      setOpen(false);
      onSuccess?.();
    },
    onError: (e) => { setServerError(String(e)); toast.error("Failed to update schedule"); },
  });

  function onSubmit(values: FormValues) {
    setServerError(null);
    if (isEdit) { updateMu.mutate(values); } else { createMu.mutate(values); }
  }

  return (
    <Dialog open={open} onOpenChange={handleOpen}>
      {!isEdit && (
        <DialogTrigger asChild>
          <Button variant="default" size="sm">New Schedule</Button>
        </DialogTrigger>
      )}
      <DialogContent className="sm:max-w-md">
        <DialogHeader>
          <DialogTitle>{isEdit ? "Edit Schedule" : "New Schedule"}</DialogTitle>
        </DialogHeader>
        <Form {...form}>
          <form onSubmit={form.handleSubmit(onSubmit)} className="space-y-4">
            <FormField control={form.control} name="name" render={({ field }) => (
              <FormItem>
                <FormLabel className={labelClass}>Name</FormLabel>
                <FormControl><Input {...field} className={fieldClass} placeholder="Daily patrol" /></FormControl>
                <FormMessage />
              </FormItem>
            )} />
            <FormField control={form.control} name="agentId" render={({ field }) => (
              <FormItem>
                <FormLabel className={labelClass}>Agent</FormLabel>
                <FormControl>
                  <Select value={field.value} onValueChange={field.onChange}>
                    <SelectTrigger className={fieldClass}><SelectValue placeholder="Select agent..." /></SelectTrigger>
                    <SelectContent>
                      {agents.map((a) => <SelectItem key={a.agentId} value={a.agentId}>{a.name}</SelectItem>)}
                    </SelectContent>
                  </Select>
                </FormControl>
                <FormMessage />
              </FormItem>
            )} />
            <FormField control={form.control} name="cronExpr" render={({ field }) => (
              <FormItem>
                <FormLabel className={labelClass}>Cron Expression</FormLabel>
                <FormControl><Input {...field} className={fieldClass} placeholder="0 9 * * *" /></FormControl>
                <p className="text-[10px] text-[var(--mute)] mt-1">5-field expression, interpreted in UTC</p>
                <FormMessage />
              </FormItem>
            )} />
            <FormField control={form.control} name="prompt" render={({ field }) => (
              <FormItem>
                <FormLabel className={labelClass}>Prompt</FormLabel>
                <FormControl><Textarea {...field} className={fieldClass} placeholder="What to tell the agent on each fire" rows={3} /></FormControl>
                <FormMessage />
              </FormItem>
            )} />
            <div className="grid grid-cols-2 gap-4">
              <FormField control={form.control} name="timeoutMs" render={({ field }) => (
                <FormItem>
                  <FormLabel className={labelClass}>Timeout (ms)</FormLabel>
                  <FormControl><Input {...field} type="number" className={fieldClass} placeholder="0" /></FormControl>
                  <p className="text-[10px] text-[var(--mute)] mt-1">0 = no per-job watchdog</p>
                  <FormMessage />
                </FormItem>
              )} />
              <FormField control={form.control} name="maxRetries" render={({ field }) => (
                <FormItem>
                  <FormLabel className={labelClass}>Max Retries</FormLabel>
                  <FormControl><Input {...field} type="number" className={fieldClass} placeholder="0" /></FormControl>
                  <p className="text-[10px] text-[var(--mute)] mt-1">0 = no retry</p>
                  <FormMessage />
                </FormItem>
              )} />
            </div>
            <FormField control={form.control} name="enabled" render={({ field }) => (
              <FormItem className="flex flex-row items-center gap-2">
                <FormControl><Checkbox checked={field.value} onCheckedChange={field.onChange} /></FormControl>
                <FormLabel className="text-sm">Enabled</FormLabel>
              </FormItem>
            )} />
            {serverError && <p className="text-xs text-destructive">{serverError}</p>}
            <Button type="submit" className="w-full" disabled={form.formState.isSubmitting || createMu.isPending || updateMu.isPending}>
              {isEdit ? "Save Changes" : "Create Schedule"} {(createMu.isPending || updateMu.isPending) && <ArrowRight className="ml-2 h-4 w-4 animate-pulse" />}
            </Button>
          </form>
        </Form>
      </DialogContent>
    </Dialog>
  );
}
```

- [ ] **Step 3: 写 page.tsx**

照 `(main)/projects/page.tsx` 模式。列表卡片每张显示 name / cronExpr / agent / enabled 徽标 + enable Switch + 编辑/删除。**点卡片跳 `/conversations/<cronJobId>`**。

```tsx
// apps/web/src/app/(main)/cron/page.tsx
"use client";

import Link from "next/link";
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { Pencil, Trash2 } from "lucide-react";
import { useState } from "react";
import { toast } from "sonner";

import { CronJobForm } from "@/components/CronJobForm";
import { Badge } from "@/components/ui/badge";
import { Breadcrumb, BreadcrumbItem, BreadcrumbList, BreadcrumbPage } from "@/components/ui/breadcrumb";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Switch } from "@/components/ui/switch";
import { api, type CronJobRow } from "@/lib/api";

export const dynamic = "force-dynamic";

export default function CronJobsPage() {
  const qc = useQueryClient();
  const { data, isLoading } = useQuery({ queryKey: ["cron-jobs"], queryFn: api.listCronJobs });
  const [editJob, setEditJob] = useState<CronJobRow | undefined>();

  const deleteMu = useMutation({
    mutationFn: api.deleteCronJob,
    onSuccess: () => { qc.invalidateQueries({ queryKey: ["cron-jobs"] }); toast.success("Schedule deleted"); },
    onError: (e) => toast.error(String(e)),
  });

  const toggleMu = useMutation({
    mutationFn: ({ id, enabled }: { id: string; enabled: boolean }) => api.setCronJobEnabled(id, enabled),
    onSuccess: () => { qc.invalidateQueries({ queryKey: ["cron-jobs"] }); },
    onError: (e) => toast.error(String(e)),
  });

  const cronJobs = data?.cronJobs ?? [];

  return (
    <div className="h-full bg-[var(--canvas)]">
      <div className="border-b border-[var(--hairline)]">
        <div className="container mx-auto px-8 py-5 flex items-center justify-between">
          <Breadcrumb>
            <BreadcrumbList>
              <BreadcrumbItem><BreadcrumbPage>Schedules</BreadcrumbPage></BreadcrumbItem>
            </BreadcrumbList>
          </Breadcrumb>
          <CronJobForm onSuccess={() => setEditJob(undefined)} />
        </div>
      </div>
      <div className="container mx-auto px-8 py-10">
        {isLoading ? (
          <p className="text-sm text-[var(--mute)]">Loading...</p>
        ) : cronJobs.length === 0 ? (
          <p className="text-sm text-[var(--mute)]">No schedules yet. Create one to get started.</p>
        ) : (
          <div className="grid gap-3">
            {cronJobs.map((job) => (
              <Card key={job.cronJobId}>
                <CardContent className="p-4 flex items-center justify-between">
                  <Link href={`/conversations/${job.cronJobId}`} className="flex-1 min-w-0">
                    <div className="text-sm font-medium truncate">{job.name}</div>
                    <div className="flex items-center gap-2 mt-1">
                      <code className="text-xs bg-[var(--canvas-soft)] px-1.5 py-0.5 rounded">{job.cronExpr}</code>
                      <span className="text-xs text-[var(--mute)]">{job.agentId}</span>
                      {job.enabled
                        ? <Badge variant="default" className="text-[10px] h-5">Active</Badge>
                        : <Badge variant="outline" className="text-[10px] h-5">Paused</Badge>}
                    </div>
                  </Link>
                  <div className="flex items-center gap-3 ml-4" onClick={(e) => e.stopPropagation()}>
                    <Switch checked={job.enabled} onCheckedChange={(v) => toggleMu.mutate({ id: job.cronJobId, enabled: v })} />
                    <Button variant="ghost" size="icon" onClick={() => setEditJob(job)}><Pencil className="h-4 w-4" /></Button>
                    <Button variant="ghost" size="icon" onClick={() => { if (confirm("Delete this schedule?")) deleteMu.mutate(job.cronJobId); }}>
                      <Trash2 className="h-4 w-4" />
                    </Button>
                  </div>
                </CardContent>
              </Card>
            ))}
          </div>
        )}
      </div>
      {/* Edit dialog — rendered outside the loop, triggered by editJob state */}
      <CronJobForm editCronJob={editJob} onSuccess={() => setEditJob(undefined)} />
    </div>
  );
}
```

> 注意：`CronJobForm` 在 edit 模式下不渲染 `DialogTrigger`（靠 `!isEdit` 条件），而是靠 `useEffect` 监听 `editCronJob` 变化自动打开 Dialog。这需要在 `CronJobForm` 里加一个「外部关闭后清 editJob」的机制——`handleOpen(false)` 时调 `onSuccess?.()` 即可。

- [ ] **Step 4: 改 NavRail.tsx — 加入口**

在 Navigate 段（`NavRail.tsx:228-237`附近）Projects 按钮之后加：

```tsx
<SidebarMenuItem>
  <SidebarMenuButton
    isActive={pathname.startsWith("/cron")}
    tooltip="Schedules"
    onClick={() => {
      closeMobile();
      router.push("/cron");
    }}
  >
    <ClockIcon />
    <span className="truncate">Schedules</span>
  </SidebarMenuButton>
</SidebarMenuItem>
```

需要在 import 里加 `Clock` icon（`lucide-react` 有 `Clock`）：

```ts
import { ..., ClockIcon } from "lucide-react";
```

- [ ] **Step 5: Typecheck + lint**

```bash
bun run --cwd apps/web typecheck
bun run --cwd apps/web lint
```

- [ ] **Step 6: Commit**

```bash
git add apps/web/src/lib/api.ts \
        apps/web/src/components/CronJobForm.tsx \
        apps/web/src/app/\(main\)/cron/page.tsx \
        apps/web/src/components/NavRail.tsx
git commit -m "feat(web): cron jobs page, form, nav entry, click-through to conversation"
```

---

### Task 7 — 收尾：文档翻 status + 全量验证 + retro

**Files:**
- Modify: `docs/architecture/foundations/cron-job.md`
- Create: `docs/superpowers/retros/2026-06-23-m21-cron-jobs.md`

- [ ] **Step 1: 文档翻 status**

把 `docs/architecture/foundations/cron-job.md` frontmatter 的 `status: design` 改成 `status: current`，补 `last_verified_against_code: 2026-06-23`。

- [ ] **Step 2: 全量验证**

```bash
bun run typecheck       # 全 workspace
bun run lint            # 全 workspace
bun test                # 全 workspace
# Expected: all pass
```

- [ ] **Step 3: 手动冒烟清单**
  - 空库启动后端：4 个 DB migration 跑通（含 `cron_job` 表 + `run_origin.cron_job_id` 列）
  - `POST /api/cron-jobs` 建 enabled job → 201，`GET /api/cron-jobs` 列出
  - 确认会话建出（`conversationId = cronJobId`）
  - `PATCH` / `POST .../enable` / `DELETE` 返回正确状态码
  - 前端 `/cron` 页列出卡片，enable Switch 生效，点卡片落 `/conversations/<id>`
  - 到点触发 → run 出现在该会话 → 前端会话页可见

- [ ] **Step 4: 写 retro**

`docs/superpowers/retros/2026-06-23-m21-cron-jobs.md`：

```markdown
# M21 CronJob Retro

**Date:** 2026-06-23
**Status:** ✅ Complete

## What went well

1. **复用现有执行层** — CronJob 不发明新的起运行/写账本/记事件机制，全部经 dispatcher → supervisor → projection。新增代码量主要集中在 feature 骨架 + scheduler（~400 行后端 + ~250 行前端）。
2. **Issue 套路直接投影** — threadId/自带会话/convPort 注入全部照搬，零学习成本。
3. **事实与投影分离** — cron_job 表是事实，Bun.cron 句柄是投影。重启后按表重建，简单可靠。

## Trade-offs

1. **Bun 版本门槛** — `Bun.cron` 需 ≥ 1.3.11，`packageManager` 从 1.1.0 升到 1.3.14。
2. **命名冲突** — Bun 的 `CronJob` 类型与 domain `CronJob` 同名，代码里用 `import { CronJob as BunCron }` 别名。
3. **at-most-once 错过语义** — 进程内定时器不跨重启，宕机期间错过的触发不补跑。M21 接受此限制。
4. **retry 监听器 onRunComplete 叠加** — 需确认 supervisor 对监听器的调用语义（串行 await vs 并行），实施时已核对。

## Investment payoff

「不发明执行机制」的投资在本次全部兑现：dispatcher/supervisor/projection 三个文件各改 ~3 行（加 originKind + 隔离 + cronJobId），其余全是增量 feature 代码。没有双写者漂移，没有第二份执行权威。
```

- [ ] **Step 5: Commit**

```bash
git add docs/architecture/foundations/cron-job.md \
        docs/superpowers/retros/2026-06-23-m21-cron-jobs.md
git commit -m "docs(cron): flip cron-job.md to current; M21 retro"
```

---

## 任务依赖图

```
Task 0 (Bun version) ──┐
                       ├──> Task 1 (entity+schema) ──> Task 2 (ports+adapter+service) ──┐
                       │                                                                  │
                       └──> Task 3 (dispatcher+projection) ──────────────────────────────┤
                                                                                          │
                                                                                          ├──> Task 5 (HTTP+main) ──> Task 6 (frontend) ──> Task 7 (docs+verify)
                                                                                          │
                                                              Task 4 (scheduler) ────────┘
```

Task 0-3 可并行（无文件冲突）。Task 4 依赖 Task 2（scheduler 引用 service types）。Task 5 依赖 Task 2+3+4。Task 6 依赖 Task 5（API 端点存在）。Task 7 收尾。
