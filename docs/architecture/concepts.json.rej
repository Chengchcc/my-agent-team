--- concepts.json
+++ concepts.json
@@ -40,7 +40,7 @@
       "status": "proposed",
       "owners": "architecture",
       "path": "docs/architecture/foundations/identifiers.md",
-      "summary": "系统里的 id 分两类：实体主键（conversationId / agentId / issueId / cronJobId / memberId，各自独立、不派生）和运行上下文 id（sessionId / runId / attemptSeq，沿一条派生链收束）。「那条持久记忆线」的本体就是 session，它在最底层 checkpointer 里只有一个 key——今天叫 threadId，应正名为 sessionId。run 是 session 上的一次 prompt loop，attempt 是 run 内的重试序号、不配独立 id。本页立下「每个 id 归属哪一层」的准则并记录现状差距。",
+      "summary": "系统里的 id 分两类：实体主键（conversationId / agentId / issueId / cronJobId / memberId，各自独立、不派生）和运行上下文 id（sessionId / spanId / attemptSeq，沿一条派生链收束）。「那条持久记忆线」的本体就是 session，它在最底层 checkpointer 里只有一个 key——今天叫 threadId，应正名为 sessionId。对齐分布式追踪：session 即 trace，span 是 session 上的一次 prompt loop（root span），step（llm_call/tool_call）是 child span，attempt 是 span 内的重试序号、不配独立 id。本页立下「每个 id 归属哪一层」的准则并记录现状差距。",
       "depends_on": ["design-philosophy", "foundations.facts-and-projections"],
       "used_by": ["harness.harness", "runtime.framework", "backend.overview"]
     },
