// Deprecated: Task management is handled via the `todo_write` tool in `../todos/todo-middleware.ts`
// This file previously contained TaskCreate/TaskUpdate/TaskGet/TaskList which used a separate global task store
// that was not integrated with the context-based system.
// Those classes have been removed as they were dead code with duplicate functionality.
